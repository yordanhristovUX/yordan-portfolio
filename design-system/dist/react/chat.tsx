/* GENERATED by scripts/build.mjs from components/chat/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/chat (dist/react/chat.tsx).
   The element comes from the canonical HTML in components/chat/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.chat`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const chat = cva([
  "grid",
  "gap-space-5",
]);

export type ChatVariants = VariantProps<typeof chat>;

export type ChatProps = ChatVariants & { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/**
 * Renders the canonical pattern of components/chat/spec.md: a <div>. Styling only — behaviour
 * is the consumer's.
 */
export function Chat({ className, ...rest }: ChatProps) {
  return <div className={cx(chat(), className)} {...rest} />;
}

export const chatThread = cva([
  "grid",
  "[gap:0]",
  "[max-height:min(46rem,_75vh)]",
  "[overflow-y:auto]",
  "[overscroll-behavior:contain]",
  "[border-top:var(--rule)]",
  /* `.chat__thread:empty` */
  "[&:empty]:hidden",
]);

export type ChatThreadProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.chat__thread` half of the pattern in components/chat/spec.md: a <div>. */
export function ChatThread({ className, ...rest }: ChatThreadProps) {
  return <div className={cx(chatThread(), className)} {...rest} />;
}

export const chatTurn = cva([
  "grid",
  "gap-space-2",
  "pt-space-5",
  "pr-pad",
  "pb-space-5",
  "pl-0",
  "[border-bottom:var(--rule)]",
  "[&:last-child]:[border-bottom:0]",
]);

export type ChatTurnProps = { className?: string } & Omit<ComponentPropsWithRef<"article">, "className">;

/** The `.chat__turn` half of the pattern in components/chat/spec.md: a <article>. */
export function ChatTurn({ className, ...rest }: ChatTurnProps) {
  return <article className={cx(chatTurn(), className)} {...rest} />;
}

/**
 * A PART AND NOT A VARIANT, and the reason is structural rather than a judgement about what a
 * user turn is: a modifier's selector is its ROOT plus its name, and this component's root is
 * `.chat`. `.chat__turn--user` modifies a part, and a part has no axis — a companion selector
 * that needs one is a component with its own definition, which is the line that has held
 * through sixteen blocks. So the two turn kinds are two parts, exactly as the stylesheet
 * writes them.
 */
export const chatTurnUser = cva([
  "bg-surface-raised",
  "[padding-left:var(--pad)]",
]);

export type ChatTurnUserProps = { className?: string } & Omit<ComponentPropsWithRef<"article">, "className">;

/** The `.chat__turn--user` half of the pattern in components/chat/spec.md: a <article>. */
export function ChatTurnUser({ className, ...rest }: ChatTurnUserProps) {
  return <article className={cx(chatTurnUser(), className)} {...rest} />;
}

export const chatRole = cva([
  "text-chrome-label",
]);

export type ChatRoleProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__role` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatRole({ className, ...rest }: ChatRoleProps) {
  return <p className={cx(chatRole(), className)} {...rest} />;
}

/**
 * The assistant turn declares nothing of its own — a SCOPE, exactly as `.drawer .profile >
 * div` is. Its whole effect is the role label inside it turning accent, which is the rule
 * below.
 */
export const chatTurnAssistant = cva([
  /* `.chat__turn--assistant .chat__role` */
  "[&_.chat__role]:text-accent",
]);

export type ChatTurnAssistantProps = { className?: string } & Omit<ComponentPropsWithRef<"article">, "className">;

/** The `.chat__turn--assistant` half of the pattern in components/chat/spec.md: a <article>. */
export function ChatTurnAssistant({ className, ...rest }: ChatTurnAssistantProps) {
  return <article className={cx(chatTurnAssistant(), className)} {...rest} />;
}

export const chatBody = cva([
  "grid",
  "gap-space-3",
  "[min-width:0]",
]);

export type ChatBodyProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.chat__body` half of the pattern in components/chat/spec.md: a <div>. */
export function ChatBody({ className, ...rest }: ChatBodyProps) {
  return <div className={cx(chatBody(), className)} {...rest} />;
}

export const chatAnswer = cva([
  "grid",
  "gap-space-3",
  "[min-width:0]",
]);

export type ChatAnswerProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.chat__answer` half of the pattern in components/chat/spec.md: a <div>. */
export function ChatAnswer({ className, ...rest }: ChatAnswerProps) {
  return <div className={cx(chatAnswer(), className)} {...rest} />;
}

export const chatProse = cva([
  "[max-width:44rem]",
  "[line-height:1.6]",
  /* `.chat__prose strong` */
  "[&_strong]:text-content-primary",
  "[&_strong]:font-semibold",
]);

export type ChatProseProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__prose` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatProse({ className, ...rest }: ChatProseProps) {
  return <p className={cx(chatProse(), className)} {...rest} />;
}

export const chatWhy = cva([
  "text-step-sm",
  "text-content-muted",
  "[padding-left:var(--space-3)]",
  "[border-left:2px_solid_var(--chrome-border-strong)]",
  "[max-width:44rem]",
]);

export type ChatWhyProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__why` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatWhy({ className, ...rest }: ChatWhyProps) {
  return <p className={cx(chatWhy(), className)} {...rest} />;
}

export const chatMetric = cva([
  "flex",
  "[align-items:center]",
  "gap-space-4",
  "flex-wrap",
  /* `.chat__metric .stat` */
  "[&_.stat]:m-0",
]);

export type ChatMetricProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__metric` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatMetric({ className, ...rest }: ChatMetricProps) {
  return <p className={cx(chatMetric(), className)} {...rest} />;
}

export const chatError = cva([
  "py-space-3",
  "px-space-4",
  "bg-chrome-bg",
  "[box-shadow:inset_0_0_0_1px_var(--chrome-border-strong)]",
  "text-step-md",
  "text-content-body",
  "[max-width:44rem]",
]);

export type ChatErrorProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__error` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatError({ className, ...rest }: ChatErrorProps) {
  return <p className={cx(chatError(), className)} {...rest} />;
}

export const chatNote = cva([
  "text-step-xs",
  "tracking-step-wide",
  "text-chrome-label",
  "[max-width:44rem]",
]);

export type ChatNoteProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__note` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatNote({ className, ...rest }: ChatNoteProps) {
  return <p className={cx(chatNote(), className)} {...rest} />;
}

export const chatState = cva([
  "flex",
  "[align-items:center]",
  "gap-space-2",
  "text-step-xs",
  "tracking-step-wide-lg",
  "uppercase",
  "text-chrome-label",
]);

export type ChatStateProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__state` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatState({ className, ...rest }: ChatStateProps) {
  return <p className={cx(chatState(), className)} {...rest} />;
}

export const chatCell = cva([
  "[width:9px]",
  "[height:9px]",
  "[flex-shrink:0]",
  "bg-primary-muted",
  "[animation:chat-life_1.6s_steps(2)_infinite]",
  "[&:nth-child(2)]:[animation-delay:0.2s]",
  "[&:nth-child(3)]:[animation-delay:0.4s]",
  "[&:nth-child(4)]:[animation-delay:0.6s]",
]);

export type ChatCellProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.chat__cell` half of the pattern in components/chat/spec.md: a <span>. */
export function ChatCell({ className, ...rest }: ChatCellProps) {
  return <span className={cx(chatCell(), className)} {...rest} />;
}

export const chatStateLabel = cva([
  "[margin-left:var(--space-2)]",
]);

export type ChatStateLabelProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.chat__state-label` half of the pattern in components/chat/spec.md: a <span>. */
export function ChatStateLabel({ className, ...rest }: ChatStateLabelProps) {
  return <span className={cx(chatStateLabel(), className)} {...rest} />;
}

export const chatTrace = cva([
  "bg-chrome-bg",
  "[box-shadow:inset_0_0_0_1px_var(--chrome-border)]",
  "font-mono",
  "text-step-2xs",
  /* `.chat__trace[open] .chat__trace-toggle::before` */
  "[&[open]_.chat__trace-toggle::before]:[content:'▾_']",
]);

export type ChatTraceProps = { className?: string } & Omit<ComponentPropsWithRef<"details">, "className">;

/** The `.chat__trace` half of the pattern in components/chat/spec.md: a <details>. */
export function ChatTrace({ className, ...rest }: ChatTraceProps) {
  return <details className={cx(chatTrace(), className)} {...rest} />;
}

export const chatTraceToggle = cva([
  "py-space-2",
  "px-space-3",
  "cursor-pointer",
  "text-chrome-label-strong",
  "tracking-step-wide",
  "uppercase",
  "[list-style:none]",
  /* `.chat__trace-toggle::-webkit-details-marker` — The vendor pseudo, as-spelled.
     `list-style: none` above is what removes the standard marker; WebKit draws its
     disclosure triangle through this one instead, and the standard `::marker` does not
     reach it. A closed enum takes the name verbatim rather than teaching the format about
     vendor prefixes. */
  "[&::-webkit-details-marker]:hidden",
  /* `.chat__trace-toggle::before` */
  "[&::before]:[content:'▸_']",
]);

export type ChatTraceToggleProps = { className?: string } & Omit<ComponentPropsWithRef<"summary">, "className">;

/** The `.chat__trace-toggle` half of the pattern in components/chat/spec.md: a <summary>. */
export function ChatTraceToggle({ className, ...rest }: ChatTraceToggleProps) {
  return <summary className={cx(chatTraceToggle(), className)} {...rest} />;
}

export const chatTraceList = cva([
  "[list-style:none]",
]);

export type ChatTraceListProps = { className?: string } & Omit<ComponentPropsWithRef<"ol">, "className">;

/** The `.chat__trace-list` half of the pattern in components/chat/spec.md: a <ol>. */
export function ChatTraceList({ className, ...rest }: ChatTraceListProps) {
  return <ol className={cx(chatTraceList(), className)} {...rest} />;
}

export const chatTraceRow = cva([
  "flex",
  "flex-wrap",
  "[align-items:baseline]",
  "gap-space-2",
  "py-space-2",
  "px-space-3",
  "[border-top:var(--rule)]",
  "text-chrome-label",
]);

export type ChatTraceRowProps = { className?: string } & Omit<ComponentPropsWithRef<"li">, "className">;

/** The `.chat__trace-row` half of the pattern in components/chat/spec.md: a <li>. */
export function ChatTraceRow({ className, ...rest }: ChatTraceRowProps) {
  return <li className={cx(chatTraceRow(), className)} {...rest} />;
}

export const chatTraceName = cva([
  "text-content-primary",
  "font-semibold",
]);

export type ChatTraceNameProps = { className?: string } & Omit<ComponentPropsWithRef<"code">, "className">;

/** The `.chat__trace-name` half of the pattern in components/chat/spec.md: a <code>. */
export function ChatTraceName({ className, ...rest }: ChatTraceNameProps) {
  return <code className={cx(chatTraceName(), className)} {...rest} />;
}

export const chatTraceArgs = cva([
  "text-chrome-label",
  "[word-break:break-word]",
  "[min-width:0]",
]);

export type ChatTraceArgsProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.chat__trace-args` half of the pattern in components/chat/spec.md: a <span>. */
export function ChatTraceArgs({ className, ...rest }: ChatTraceArgsProps) {
  return <span className={cx(chatTraceArgs(), className)} {...rest} />;
}

export const chatTraceResult = cva([
  "text-content-body",
]);

export type ChatTraceResultProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.chat__trace-result` half of the pattern in components/chat/spec.md: a <span>. */
export function ChatTraceResult({ className, ...rest }: ChatTraceResultProps) {
  return <span className={cx(chatTraceResult(), className)} {...rest} />;
}

export const chatTraceMs = cva([
  "[margin-left:auto]",
  "text-chrome-label",
  /* Under `@media (max-width: 560px)` (`below-560`) */
  "[@media(max-width:560px)]:[margin-left:0]",
]);

export type ChatTraceMsProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.chat__trace-ms` half of the pattern in components/chat/spec.md: a <span>. */
export function ChatTraceMs({ className, ...rest }: ChatTraceMsProps) {
  return <span className={cx(chatTraceMs(), className)} {...rest} />;
}

export const chatForm = cva([
  "grid",
  "[grid-template-columns:1fr_auto]",
  "gap-space-3",
  "[align-items:end]",
  /* Under `@media (max-width: 560px)` (`below-560`) */
  "[@media(max-width:560px)]:[grid-template-columns:1fr]",
]);

export type ChatFormProps = { className?: string } & Omit<ComponentPropsWithRef<"form">, "className">;

/** The `.chat__form` half of the pattern in components/chat/spec.md: a <form>. */
export function ChatForm({ className, ...rest }: ChatFormProps) {
  return <form className={cx(chatForm(), className)} {...rest} />;
}

export const chatInput = cva([
  "[font:inherit]",
  "text-step-md",
  "text-content-body",
  "bg-surface-page",
  "border",
  "border-solid",
  "border-chrome-border-strong",
  "p-space-3",
  "[resize:vertical]",
  "[min-height:3rem]",
  "[width:100%]",
  "focus-visible:border-accent",
  "[&[aria-disabled='true']]:text-content-muted",
  /* `.chat__input::placeholder` */
  "[&::placeholder]:text-chrome-label",
]);

export type ChatInputProps = { className?: string } & Omit<ComponentPropsWithRef<"textarea">, "className">;

/** The `.chat__input` half of the pattern in components/chat/spec.md: a <textarea>. */
export function ChatInput({ className, ...rest }: ChatInputProps) {
  return <textarea className={cx(chatInput(), className)} {...rest} />;
}

export const chatStatus = cva([
  "text-step-xs",
  "text-chrome-label",
]);

export type ChatStatusProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.chat__status` half of the pattern in components/chat/spec.md: a <p>. */
export function ChatStatus({ className, ...rest }: ChatStatusProps) {
  return <p className={cx(chatStatus(), className)} {...rest} />;
}

export const chatSuggest = cva([
  "flex",
  "flex-wrap",
  "gap-space-2",
]);

export type ChatSuggestProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.chat__suggest` half of the pattern in components/chat/spec.md: a <div>. */
export function ChatSuggest({ className, ...rest }: ChatSuggestProps) {
  return <div className={cx(chatSuggest(), className)} {...rest} />;
}

/**
 * QUERY-ONLY. The submit button is a `.btn.btn--solid` at every width and the composer's own
 * class says one thing about it and only on a phone: span the row it now has to itself. There
 * is no unconditional `.chat__send` rule for an override to name, which is the construct `nav`
 * forced.
 */
export const chatSend = cva([
  /* Under `@media (max-width: 560px)` (`below-560`), declared there and nowhere else —
     `.chat__send` — QUERY-ONLY. The submit button is a `.btn.btn--solid` at every width and
     the composer's own class says one thing about it and only on a phone: span the row it
     now has to itself. There is no unconditional `.chat__send` rule for an override to
     name, which is the construct `nav` forced. */
  "[@media(max-width:560px)]:[width:100%]",
]);

export type ChatSendProps = { className?: string } & Omit<ComponentPropsWithRef<"button">, "className">;

/** The `.chat__send` half of the pattern in components/chat/spec.md: a <button>. */
export function ChatSend({ className, ...rest }: ChatSendProps) {
  return <button className={cx(chatSend(), className)} {...rest} />;
}
