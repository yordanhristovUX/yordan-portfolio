/* GENERATED by scripts/build.mjs from components/ask-fab/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/ask-fab (dist/react/ask-fab.tsx).
   The element comes from the canonical HTML in components/ask-fab/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.ask-fab`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const askFab = cva([
  "[position:fixed]",
  "[bottom:1rem]",
  "[right:1rem]",
  "[z-index:90]",
  "hidden",
  "[align-items:center]",
  "gap-space-2",
  "pt-[2px]",
  "pr-space-4",
  "pb-[2px]",
  "pl-[2px]",
  "[border-radius:999px]",
  "bg-accent",
  "text-content-inverse",
  "font-mono",
  "text-step-xs",
  "tracking-step-wide-sm",
  "whitespace-nowrap",
  "[box-shadow:2px_2px_0_var(--shadow-drop)]",
  "[transition:gap_0.28s,_padding_0.28s,_transform_0.2s,_background-color_0.2s]",
  /* Under `@media (max-width: 699px)` (`below-699`) — MOBILE ONLY, and the number is the
     one the bar gives up at. 699 rather than 700 because the bar's own query claims 700 and
     above; the two are one decision written from both sides, which is exactly the drift a
     named condition closes — `below-699` is consumed here and by nav, and neither block can
     move it alone. */
  "[@media(max-width:699px)]:flex",
  "[&:hover]:bg-primary",
  "[&:hover]:[transform:translateY(-1px)]",
  "[&[data-collapsed]]:[gap:0]",
  "[&[data-collapsed]]:p-[2px]",
  /* `.ask-fab[data-collapsed] .ask-fab__label` — The label, folded away. Scoped to the
     STATE rather than given a modifier, which is the shape only a named reference can reach
     — `.ask-fab[data-collapsed]` owns no selector an author could quote, and this is the
     third block to need that (media's `.ph:has(img) .ph__label` and drawer's sheet are the
     others). The accessible name does not fold with it: it is an `aria-label` on the button
     and spec.md is where that rule lives. */
  String.raw`[&[data-collapsed]_.ask-fab\_\_label]:[max-width:0]`,
  String.raw`[&[data-collapsed]_.ask-fab\_\_label]:[opacity:0]`,
]);

export type AskFabVariants = VariantProps<typeof askFab>;

export type AskFabProps = AskFabVariants & { className?: string } & Omit<ComponentPropsWithRef<"button">, "className">;

/**
 * Renders the canonical pattern of components/ask-fab/spec.md: a <button>. Styling only —
 * behaviour is the consumer's.
 */
export function AskFab({ className, ...rest }: AskFabProps) {
  return <button className={cx(askFab(), className)} {...rest} />;
}

/**
 * The assistant's portrait, filling the pill. A part rather than a variant because it is a
 * second element inside the button, and it is the only thing left once the label folds.
 */
export const askFabFace = cva([
  "block",
  "[width:2.75rem]",
  "[height:2.75rem]",
  "[flex:none]",
  "[border-radius:50%]",
]);

export type AskFabFaceProps = { className?: string } & Omit<ComponentPropsWithRef<"img">, "className">;

/** The `.ask-fab__face` half of the pattern in components/ask-fab/spec.md: a <img>. */
export function AskFabFace({ className, ...rest }: AskFabFaceProps) {
  return <img className={cx(askFabFace(), className)} {...rest} />;
}

/**
 * The words. Its note is a RULE note rather than a group note because it explains the whole
 * rule — the choice of max-width, the ceiling, and what sets the speed — and the stylesheet
 * writes it above the selector, which is the distinction the two note keys record.
 */
export const askFabLabel = cva([
  "[max-width:12rem]",
  "[overflow:hidden]",
  "[opacity:1]",
  "[transition:max-width_0.28s_cubic-bezier(0.22,_1,_0.36,_1),_opacity_0.2s]",
]);

export type AskFabLabelProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.ask-fab__label` half of the pattern in components/ask-fab/spec.md: a <span>. */
export function AskFabLabel({ className, ...rest }: AskFabLabelProps) {
  return <span className={cx(askFabLabel(), className)} {...rest} />;
}
