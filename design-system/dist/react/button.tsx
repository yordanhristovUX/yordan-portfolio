/* GENERATED by scripts/build.mjs from components/button/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/button (dist/react/button.tsx).
   The element comes from the canonical HTML in components/button/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.btn`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const button = cva(
  [
    "inline-block",
    "font-mono",
    "font-semibold",
    "tracking-[0.05em]",
    "border",
    "border-solid",
    "border-content-primary",
    "text-center",
    "[transition:background-color_0.2s,_color_0.2s]",
    "whitespace-nowrap",
    "cursor-pointer",
    "[&:hover]:text-content-inverse",
  ],
  {
    variants: {
      variant: {
        /**
         * The base's own value for every property this axis overrides. It lives here rather
         * than in the base list because a class attribute has no order — see the transform in
         * scripts/emit-react.mjs.
         */
        default: [
          "text-content-primary",
          "[&:hover]:bg-primary",
        ],
        /**
         * THE primary action, at most one per view — asserted per shipped page by
         * scripts/check-css.mjs rule 5. It is the resting state of the outline button's hover,
         * which is why the two share `primary` and `content-inverse` rather than each naming a
         * colour.
         */
        solid: [
          "bg-primary",
          "text-content-inverse",
          "[&:hover]:bg-action-hover",
        ],
      },
      size: {
        /**
         * The base's own value for every property this axis overrides. It lives here rather
         * than in the base list because a class attribute has no order — see the transform in
         * scripts/emit-react.mjs.
         */
        default: [
          "text-step-xs",
          "py-space-3",
          "px-space-5",
        ],
        /**
         * Chrome contexts — a dialog bar, a dense row. One step down the padding ramp and one
         * step down the type ramp; it changes nothing else, which is why it is a size and not
         * a variant.
         */
        small: [
          "py-space-2",
          "px-space-3",
          "text-step-2xs",
        ],
      },
    },
    defaultVariants: { variant: "default", size: "default" },
  }
);

export type ButtonVariants = VariantProps<typeof button>;

/**
 * The props of `<Button />`. A union rather than one type, because the canonical HTML in
 * components/button/spec.md renders this on <a> AND on <button>: pass `href` and it is a link,
 * omit it and it is a button, and the element's own attributes follow the branch you chose.
 */
export type ButtonProps =
  | (ButtonVariants & { className?: string } & { href: string } & Omit<ComponentPropsWithRef<"a">, "className" | "href">)
  | (ButtonVariants & { className?: string } & { href?: undefined } & Omit<ComponentPropsWithRef<"button">, "className">);

/**
 * Renders the canonical pattern of components/button/spec.md. Styling only — behaviour is the
 * consumer's.
 */
export function Button(props: ButtonProps) {
  if (props.href !== undefined) {
    const { variant, size, className, ...rest } = props;
    return <a className={cx(button({ variant, size }), className)} {...rest} />;
  }
  const { variant, size, className, ...rest } = props;
  return <button className={cx(button({ variant, size }), className)} {...rest} />;
}
