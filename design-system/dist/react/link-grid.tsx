/* GENERATED by scripts/build.mjs from components/link-grid/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/link-grid (dist/react/link-grid.tsx).
   The element comes from the canonical HTML in components/link-grid/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.link-grid`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const linkGrid = cva([
  "flex",
  "flex-wrap",
  "[border:var(--rule)]",
  "[border-width:1px_0_0_1px]",
  /* `.link-grid a` — One channel. `flex: 1 1 11rem` is the whole responsive behaviour —
     cells share the row until one would be narrower than 11rem, and then the row wraps;
     there is no breakpoint here and there is nothing for one to do. Its border is the
     mirror of the root's: right and bottom, so the two together draw a closed grid whatever
     the wrap lands on. */
  "[&_a]:[flex:1_1_11rem]",
  "[&_a]:py-space-4",
  "[&_a]:px-pad",
  "[&_a]:font-mono",
  "[&_a]:text-step-xs",
  "[&_a]:font-medium",
  "[&_a]:tracking-step-wide",
  "[&_a]:text-chrome-label-strong",
  "[&_a]:[border:var(--rule)]",
  "[&_a]:[border-width:0_1px_1px_0]",
  "[&_a]:[transition:background-color_0.2s,_color_0.2s]",
  "[&_a:hover]:bg-primary",
  "[&_a:hover]:text-content-inverse",
]);

export type LinkGridVariants = VariantProps<typeof linkGrid>;

export type LinkGridProps = LinkGridVariants & { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/**
 * Renders the canonical pattern of components/link-grid/spec.md: a <div>. Styling only —
 * behaviour is the consumer's.
 */
export function LinkGrid({ className, ...rest }: LinkGridProps) {
  return <div className={cx(linkGrid(), className)} {...rest} />;
}
