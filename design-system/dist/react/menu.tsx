/* GENERATED by scripts/build.mjs from components/menu/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/menu (dist/react/menu.tsx).
   The element comes from the canonical HTML in components/menu/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.menu`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const menu = cva([
  "[position:fixed]",
  "[inset:0]",
  "[z-index:300]",
  "[visibility:hidden]",
  "[transition:visibility_0s_linear_var(--motion-arrive)]",
  "[&[data-open]]:[visibility:visible]",
  "[&[data-open]]:[transition-delay:0s]",
  /* `.menu[data-open] .menu__sheet` */
  String.raw`[&[data-open]_.menu\_\_sheet]:[opacity:1]`,
  String.raw`[&[data-open]_.menu\_\_sheet]:[transform:none]`,
]);

export type MenuVariants = VariantProps<typeof menu>;

export type MenuProps = MenuVariants & { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/**
 * Renders the canonical pattern of components/menu/spec.md: a <div>. Styling only — behaviour
 * is the consumer's.
 */
export function Menu({ className, ...rest }: MenuProps) {
  return <div className={cx(menu(), className)} {...rest} />;
}

export const menuSheet = cva([
  "[position:absolute]",
  "[inset:0]",
  "grid",
  "[grid-template-rows:auto_minmax(0,_1fr)]",
  "bg-surface-page",
  "[opacity:0]",
  "[transform:translateY(-0.75rem)]",
  "[transition:opacity_var(--motion-arrive)_var(--ease-fade),_transform_var(--motion-arrive)_var(--ease-arrive)]",
]);

export type MenuSheetProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.menu__sheet` half of the pattern in components/menu/spec.md: a <div>. */
export function MenuSheet({ className, ...rest }: MenuSheetProps) {
  return <div className={cx(menuSheet(), className)} {...rest} />;
}

export const menuHead = cva([
  "flex",
  "[align-items:stretch]",
  "[justify-content:space-between]",
  "font-mono",
  "text-step-xs",
  "tracking-step-wide-sm",
  "bg-chrome-bg",
  "[border-bottom:var(--rule-strong)]",
]);

export type MenuHeadProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.menu__head` half of the pattern in components/menu/spec.md: a <div>. */
export function MenuHead({ className, ...rest }: MenuHeadProps) {
  return <div className={cx(menuHead(), className)} {...rest} />;
}

export const menuTitle = cva([
  "flex",
  "[align-items:center]",
  "py-space-3",
  "px-space-5",
  "text-step-sm",
  "text-chrome-label",
]);

export type MenuTitleProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.menu__title` half of the pattern in components/menu/spec.md: a <span>. */
export function MenuTitle({ className, ...rest }: MenuTitleProps) {
  return <span className={cx(menuTitle(), className)} {...rest} />;
}

export const menuClose = cva([
  "flex",
  "[align-items:center]",
  "py-0",
  "px-space-5",
  "bg-primary",
  "text-content-inverse",
  "[box-shadow:inset_1px_0_0_var(--chrome-border)]",
  "[transition:background-color_var(--motion-state),_color_var(--motion-state)]",
  "[&:hover]:bg-action-hover",
]);

export type MenuCloseProps = { className?: string } & Omit<ComponentPropsWithRef<"button">, "className">;

/** The `.menu__close` half of the pattern in components/menu/spec.md: a <button>. */
export function MenuClose({ className, ...rest }: MenuCloseProps) {
  return <button className={cx(menuClose(), className)} {...rest} />;
}

export const menuBody = cva([
  "flex",
  "[flex-direction:column]",
  "[justify-content:center]",
  "py-space-5",
  "px-space-6",
  "[overflow-y:auto]",
  "[overscroll-behavior:contain]",
]);

export type MenuBodyProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.menu__body` half of the pattern in components/menu/spec.md: a <div>. */
export function MenuBody({ className, ...rest }: MenuBodyProps) {
  return <div className={cx(menuBody(), className)} {...rest} />;
}

export const menuNav = cva([
  "flex",
  "[flex-direction:column]",
  /* `.menu__nav a` */
  "[&_a]:font-display",
  "[&_a]:text-step-title",
  "[&_a]:[line-height:1.2]",
  "[&_a]:text-content-primary",
  "[&_a]:py-space-4",
  "[&_a]:px-0",
  "[&_a]:[transition:color_var(--motion-state)]",
  "[&_a:hover]:text-accent",
]);

export type MenuNavProps = { className?: string } & Omit<ComponentPropsWithRef<"nav">, "className">;

/** The `.menu__nav` half of the pattern in components/menu/spec.md: a <nav>. */
export function MenuNav({ className, ...rest }: MenuNavProps) {
  return <nav className={cx(menuNav(), className)} {...rest} />;
}
