/* GENERATED by scripts/build.mjs from components/profile/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/profile (dist/react/profile.tsx).
   The element comes from the canonical HTML in components/profile/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.profile`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const profile = cva([
  "grid",
  "[grid-template-columns:1fr_1fr]",
  "[border-top:var(--rule)]",
  "font-mono",
  "text-step-xs",
  /* `.profile > div` — One label-and-value pair, which the markup wraps in a <div> so that
     the outer grid has a single box to place. It is a grid of its own — a fixed label track
     and a fluid value — so every label in both columns lines up on the same edge. A CHILD
     and not a descendant: a <div> inside a value is content, not a row, and a descendant
     selector would lay it out as one. */
  "[&>div]:grid",
  "[&>div]:[grid-template-columns:7rem_1fr]",
  "[&>div]:gap-space-4",
  "[&>div]:py-space-3",
  "[&>div]:px-0",
  "[&>div]:[border-bottom:var(--rule)]",
  "[&>div:nth-child(odd)]:[padding-right:var(--space-7)]",
  /* `.profile dt` — The label. Uppercase mono at the label ink — the chrome voice, so the
     value beside it reads as the content and the term as its heading. */
  "[&_dt]:text-chrome-label",
  "[&_dt]:font-medium",
  "[&_dt]:tracking-step-wide-lg",
  "[&_dt]:uppercase",
  /* `.profile dd` — The fact itself, at body ink. One declaration, and the <dd>'s own
     margin is already off in the foundation reset. */
  "[&_dd]:text-content-body",
  /* `.profile .is-ok` — A positive status value — one per list, and spec.md is where that
     rule lives because it is about markup rather than appearance. `.is-ok` is a state class
     the page owns rather than this component, which is why it is a scoped part on a foreign
     class: the profile decides what an affirmative value looks like inside it, and does not
     decide what is affirmative. */
  "[&_.is-ok]:text-accent",
  "[&_.is-ok]:font-semibold",
  /* Under `@media (max-width: 860px)` (`below-860`) — One column. The gutter goes with the
     second column it was separating — the same two facts as above, stated the other way
     round, which is what makes this an override of named rules rather than a second layout. */
  "[@media(max-width:860px)]:[grid-template-columns:1fr]",
  /* Under `@media (max-width: 860px)` (`below-860`) — One column. The gutter goes with the
     second column it was separating — the same two facts as above, stated the other way
     round, which is what makes this an override of named rules rather than a second layout. */
  "[@media(max-width:860px)]:[&>div:nth-child(odd)]:[padding-right:0]",
]);

export type ProfileVariants = VariantProps<typeof profile>;

export type ProfileProps = ProfileVariants & { className?: string } & Omit<ComponentPropsWithRef<"dl">, "className">;

/**
 * Renders the canonical pattern of components/profile/spec.md: a <dl>. Styling only —
 * behaviour is the consumer's.
 */
export function Profile({ className, ...rest }: ProfileProps) {
  return <dl className={cx(profile(), className)} {...rest} />;
}
