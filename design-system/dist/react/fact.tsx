/* GENERATED by scripts/build.mjs from components/fact/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/fact (dist/react/fact.tsx).
   The element comes from the canonical HTML in components/fact/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.facts`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const fact = cva([
  "flex",
  "flex-wrap",
  "[gap:0]",
  "[border:var(--rule-strong)]",
  "bg-surface-page",
]);

export type FactVariants = VariantProps<typeof fact>;

export type FactProps = FactVariants & { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/**
 * Renders the canonical pattern of components/fact/spec.md: a <div>. Styling only — behaviour
 * is the consumer's.
 */
export function Fact({ className, ...rest }: FactProps) {
  return <div className={cx(fact(), className)} {...rest} />;
}

/**
 * One figure. `flex: 1 1 14rem` is the whole responsive behaviour above the breakpoint — facts
 * share the row until one would be narrower than 14rem, and then they wrap.
 */
export const factFact = cva([
  "[flex:1_1_14rem]",
  "p-space-6",
  "[border-right:var(--rule)]",
  "flex",
  "[flex-direction:column]",
  "gap-space-2",
  "[&:last-child]:[border-right:0]",
  /* Under `@media (max-width: 720px)` (`below-720`) — Stacked, the divider turns ninety
     degrees. It is the same two rules as above — every fact draws one, the last one does
     not — with the edge changed, which is what makes this an override of a named rule
     rather than a second definition of it. */
  "[@media(max-width:720px)]:[border-right:0]",
  "[@media(max-width:720px)]:[border-bottom:var(--rule)]",
  "[@media(max-width:720px)]:[&:last-child]:[border-bottom:0]",
]);

export type FactFactProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.fact` half of the pattern in components/fact/spec.md: a <div>. */
export function FactFact({ className, ...rest }: FactFactProps) {
  return <div className={cx(factFact(), className)} {...rest} />;
}

/**
 * The figure itself, at the display step and the widest step of the display family's width
 * axis. `tabular-nums` so a column of figures lines up on the digit rather than on the glyph.
 */
export const factNum = cva([
  "font-display",
  "font-black",
  "[font-variation-settings:var(--width-hero)]",
  "text-step-display",
  "[line-height:1]",
  "text-content-primary",
  "[font-variant-numeric:tabular-nums]",
  /* `.fact__num small` — The suffix riding on the number — the % or the ×. It binds
     --text-unit, which is an em RATIO rather than a step: it means "a fraction of whatever
     set me", so it follows the display number through every clamp the number goes through. */
  "[&_small]:[font-size:var(--text-unit)]",
  "[&_small]:font-bold",
  "[&_small]:text-content-muted",
  "[&_small]:[margin-left:var(--space-1)]",
]);

export type FactNumProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.fact__num` half of the pattern in components/fact/spec.md: a <span>. */
export function FactNum({ className, ...rest }: FactNumProps) {
  return <span className={cx(factNum(), className)} {...rest} />;
}

/**
 * The mono chrome label above the figure — the same family, case and tracking step as a chip,
 * a plate label and the sources heading.
 */
export const factTitle = cva([
  "font-mono",
  "text-step-2xs",
  "font-semibold",
  "tracking-step-wide-2xl",
  "uppercase",
  "text-chrome-label-strong",
]);

export type FactTitleProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.fact__title` half of the pattern in components/fact/spec.md: a <span>. */
export function FactTitle({ className, ...rest }: FactTitleProps) {
  return <span className={cx(factTitle(), className)} {...rest} />;
}

/**
 * The sentence under the figure. Body prose at the small step, so a fact reads as a claim with
 * a number rather than as a number with a caption.
 */
export const factLabel = cva([
  "text-step-sm",
  "text-content-body",
  "[line-height:1.45]",
]);

export type FactLabelProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.fact__label` half of the pattern in components/fact/spec.md: a <span>. */
export function FactLabel({ className, ...rest }: FactLabelProps) {
  return <span className={cx(factLabel(), className)} {...rest} />;
}
