/* GENERATED by scripts/build.mjs from components/theme-toggle/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/theme-toggle (dist/react/theme-toggle.tsx).
   The element comes from the canonical HTML in components/theme-toggle/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.theme`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const themeToggle = cva([
  "[position:absolute]",
  "[left:calc(100%_+_0.75rem)]",
  "[top:50%]",
  "[margin-top:-1.25rem]",
  "[z-index:90]",
  "flex",
  "[align-items:center]",
  "[justify-content:center]",
  "[width:2.5rem]",
  "[height:2.5rem]",
  "[border-radius:50%]",
  "bg-chrome-bg",
  "text-chrome-label",
  "[box-shadow:inset_0_0_0_1px_var(--chrome-border),_2px_2px_0_var(--shadow-drop)]",
  "[transition:background-color_var(--motion-state),_transform_var(--motion-state)]",
  "[&:hover]:[transform:translateY(-1px)]",
  /* `.theme[data-state="dark"] .theme__lamp` */
  String.raw`[&[data-state='dark']_.theme\_\_lamp]:bg-content-primary`,
  /* `.theme[data-state="auto"] .theme__lamp` */
  String.raw`[&[data-state='auto']_.theme\_\_lamp]:[background:linear-gradient(90deg,_var(--content-primary)_0_50%,_var(--surface-page)_50%_100%)]`,
  String.raw`[&[data-state='auto']_.theme\_\_lamp]:[animation:theme-dial_9s_linear_infinite]`,
]);

export type ThemeToggleVariants = VariantProps<typeof themeToggle>;

export type ThemeToggleProps = ThemeToggleVariants & { className?: string } & Omit<ComponentPropsWithRef<"button">, "className">;

/**
 * Renders the canonical pattern of components/theme-toggle/spec.md: a <button>. Styling only —
 * behaviour is the consumer's.
 */
export function ThemeToggle({ className, ...rest }: ThemeToggleProps) {
  return <button className={cx(themeToggle(), className)} {...rest} />;
}

export const themeToggleLamp = cva([
  "[width:14px]",
  "[height:14px]",
  "[flex-shrink:0]",
  "[border-radius:50%]",
  "bg-surface-page",
  "[box-shadow:inset_0_0_0_1px_var(--content-primary)]",
  "[transition:background_0.3s,_box-shadow_var(--motion-state)]",
]);

export type ThemeToggleLampProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.theme__lamp` half of the pattern in components/theme-toggle/spec.md: a <span>. */
export function ThemeToggleLamp({ className, ...rest }: ThemeToggleLampProps) {
  return <span className={cx(themeToggleLamp(), className)} {...rest} />;
}
