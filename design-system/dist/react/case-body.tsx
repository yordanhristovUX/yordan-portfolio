/* GENERATED by scripts/build.mjs from components/case-body/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/case-body (dist/react/case-body.tsx).
   The element comes from the canonical HTML in components/case-body/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.case-body`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const caseBody = cva([
  /* `.case-body h3` — A section heading, ruled off above so the sections read as a stack of
     plates. <h3> because the page's <h1> is the project title and the section head above
     carries the number — the level is an outline decision, and changing it breaks the
     outline before it breaks anything visible. */
  "[&_h3]:font-display",
  "[&_h3]:font-extrabold",
  "[&_h3]:[font-variation-settings:var(--width-sub)]",
  "[&_h3]:text-step-sub",
  "[&_h3]:uppercase",
  "[&_h3]:text-content-primary",
  "[&_h3]:mt-space-7",
  "[&_h3]:mx-0",
  "[&_h3]:mb-space-3",
  "[&_h3]:[padding-top:var(--space-7)]",
  "[&_h3]:[border-top:var(--rule)]",
  "[&_h3:first-child]:[margin-top:0]",
  "[&_h3:first-child]:[padding-top:0]",
  "[&_h3:first-child]:[border-top:0]",
  /* `.case-body p` — Body copy, held to a measure rather than to the well's full width. The
     measure is deliberate and narrower than the well: a case study is read, and the well is
     sized for an index that is scanned. */
  "[&_p]:[margin-bottom:var(--space-4)]",
  "[&_p]:[max-width:44rem]",
  /* `.case-body p strong` — Lifts to the primary ink; the prose around it sits at
     --content-body. A two-name element path, which is what the array form is for — the
     emitter supplies the combinator, so a descendant is the only relation an author can
     write. */
  "[&_p_strong]:text-content-primary",
  "[&_p_strong]:font-semibold",
  /* `.case-body ul` — The native marker off, so the drawn one below is the only one. */
  "[&_ul]:[list-style:none]",
  "[&_ul]:[margin-bottom:var(--space-4)]",
  /* `.case-body li` — `position: relative` is here for the marker below it, which is
     absolutely positioned against this box; the left padding is the space that marker sits
     in. */
  "[&_li]:pt-space-2",
  "[&_li]:pr-0",
  "[&_li]:pb-space-2",
  "[&_li]:pl-space-6",
  "[&_li]:[position:relative]",
  "[&_li]:[border-bottom:var(--rule)]",
  "[&_li]:[max-width:44rem]",
  /* `.case-body li::before` — The list mark. Drawn with ::before rather than written into
     the text, so it is decoration and a screen reader does not read it out — which is the
     whole reason the content pipeline emits a plain <li>. Small and in the brand ink, so a
     list reads as a set of marks rather than as punctuation. */
  "[&_li::before]:[content:'▪']",
  "[&_li::before]:[position:absolute]",
  "[&_li::before]:[left:0]",
  "[&_li::before]:[top:var(--space-2)]",
  "[&_li::before]:text-primary",
  "[&_li::before]:text-step-2xs",
  /* `.case-body li strong` — The same lift inside a list item. Written twice rather than as
     one rule under two selectors, because that is what the stylesheet says and this is a
     transcription — the two are a paragraph apart and each belongs beside the element it
     qualifies. */
  "[&_li_strong]:text-content-primary",
  "[&_li_strong]:font-semibold",
]);

export type CaseBodyVariants = VariantProps<typeof caseBody>;

export type CaseBodyProps = CaseBodyVariants & { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/**
 * Renders the canonical pattern of components/case-body/spec.md: a <div>. Styling only —
 * behaviour is the consumer's.
 */
export function CaseBody({ className, ...rest }: CaseBodyProps) {
  return <div className={cx(caseBody(), className)} {...rest} />;
}
