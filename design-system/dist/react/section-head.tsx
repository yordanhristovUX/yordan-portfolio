/* GENERATED by scripts/build.mjs from components/section-head/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/section-head (dist/react/section-head.tsx).
   The element comes from the canonical HTML in components/section-head/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.sec`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const sectionHead = cva(
  [
    "[border-top:var(--rule-strong)]",
  ],
  {
    variants: {
      variant: {
        default: "",
        /**
         * A tinted section. It declares NOTHING on itself: the whole effect is the raised
         * surface behind the well inside it, which is the `tinted-well` part below. A variant
         * rather than a part with a `--` in its name, because that is what it is on both
         * surfaces — the markup wears it beside `.sec` and the React prop is `variant="tint"`,
         * whichever element the one declaration eventually lands on.
         */
        tint: [
          /* `.sec--tint .well` — What the tint actually tints. `.well` is skeleton's class
             and not this component's, which is the honest shape of the rule: a tinted
             section raises the surface of the well inside it, and no well knows what kind
             of section it is in. Scoped to the variant rather than to the root, so it is
             the tint that carries the coupling and not the head. */
          "[&_.well]:bg-surface-raised",
        ],
      },
    },
    defaultVariants: { variant: "default" },
  }
);

export type SectionHeadVariants = VariantProps<typeof sectionHead>;

export type SectionHeadProps = SectionHeadVariants & { className?: string } & Omit<ComponentPropsWithRef<"section">, "className">;

/**
 * Renders the canonical pattern of components/section-head/spec.md: a <section>. Styling only
 * — behaviour is the consumer's.
 */
export function SectionHead({ variant, className, ...rest }: SectionHeadProps) {
  return <section className={cx(sectionHead({ variant }), className)} {...rest} />;
}

/**
 * The chrome band itself: a two-track grid, title against note, baseline-aligned so the two
 * read as one line rather than as a heading with a caption beside it.
 */
export const sectionHeadHead = cva([
  "grid",
  "[grid-template-columns:1fr_auto]",
  "[align-items:baseline]",
  "gap-space-6",
  "pt-[calc(var(--space-3)_-_2px)]",
  "px-pad",
  "pb-[calc(var(--space-3)_-_1px)]",
  "[border-bottom:var(--rule)]",
  "bg-chrome-bg",
]);

export type SectionHeadHeadProps = { className?: string } & Omit<ComponentPropsWithRef<"header">, "className">;

/** The `.sec__head` half of the pattern in components/section-head/spec.md: a <header>. */
export function SectionHeadHead({ className, ...rest }: SectionHeadHeadProps) {
  return <header className={cx(sectionHeadHead(), className)} {...rest} />;
}

/**
 * One declaration, and it is a grid fact rather than a typographic one — the voice of a
 * section title is `.t-title`, which is the typography layer's and is applied beside this
 * class rather than by it.
 */
export const sectionHeadTitle = cva([
  "[min-width:0]",
]);

export type SectionHeadTitleProps = { className?: string } & Omit<ComponentPropsWithRef<"h2">, "className">;

/** The `.sec__title` half of the pattern in components/section-head/spec.md: a <h2>. */
export function SectionHeadTitle({ className, ...rest }: SectionHeadTitleProps) {
  return <h2 className={cx(sectionHeadTitle(), className)} {...rest} />;
}

/**
 * The optional hint at the right of the head. Chrome: the smallest step above the floor of the
 * ramp, the label ink, and centred against the title's baseline row rather than sharing it,
 * because a one-line hint beside a display line looks pinned rather than aligned.
 */
export const sectionHeadNote = cva([
  "text-step-xs",
  "text-chrome-label",
  "[align-self:center]",
  /* Under `@media (max-width: 640px)` (`below-640`) — Gone, not wrapped: the note is the
     second thing in a two-track grid and there is no width left for it. That is why spec.md
     says it may never carry essential information — a rule the markup cannot enforce and
     this declaration is the reason for. */
  "[@media(max-width:640px)]:hidden",
]);

export type SectionHeadNoteProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.sec__note` half of the pattern in components/section-head/spec.md: a <span>. */
export function SectionHeadNote({ className, ...rest }: SectionHeadNoteProps) {
  return <span className={cx(sectionHeadNote(), className)} {...rest} />;
}
