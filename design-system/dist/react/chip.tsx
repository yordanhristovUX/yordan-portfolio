/* GENERATED by scripts/build.mjs from components/chip/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/chip (dist/react/chip.tsx).
   The element comes from the canonical HTML in components/chip/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.chip`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const chip = cva(
  [
    "inline-block",
    "font-mono",
    "text-step-2xs",
    "font-medium",
    "tracking-[0.06em]",
    "uppercase",
    "py-space-1",
    "px-space-2",
    "border",
    "border-solid",
  ],
  {
    variants: {
      variant: {
        /**
         * The base's own value for every property this axis overrides. It lives here rather
         * than in the base list because a class attribute has no order — see the transform in
         * scripts/emit-react.mjs.
         */
        default: [
          "border-chrome-border-strong",
          "text-chrome-label-strong",
        ],
        /**
         * The ONE emphasised fact in a set. It repaints the border as well as the fill — a
         * solid chip keeping the default border would read as a chip with an outline round its
         * ink.
         */
        solid: [
          "bg-primary",
          "border-primary",
          "text-content-inverse",
        ],
      },
    },
    defaultVariants: { variant: "default" },
  }
);

export type ChipVariants = VariantProps<typeof chip>;

export type ChipProps = ChipVariants & { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/**
 * Renders the canonical pattern of components/chip/spec.md: a <span>. Styling only — behaviour
 * is the consumer's.
 */
export function Chip({ variant, className, ...rest }: ChipProps) {
  return <span className={cx(chip({ variant }), className)} {...rest} />;
}

/**
 * The wrapper a set of chips sits in. It provides wrap and gap and nothing else — no chip
 * knows how many siblings it has, which is what keeps the same chip usable in a work-row's tag
 * list and on a project page.
 */
export const chipGroup = cva([
  "flex",
  "flex-wrap",
  "gap-space-2",
]);

export type ChipGroupProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.chips` half of the pattern in components/chip/spec.md: a <div>. */
export function ChipGroup({ className, ...rest }: ChipGroupProps) {
  return <div className={cx(chipGroup(), className)} {...rest} />;
}
