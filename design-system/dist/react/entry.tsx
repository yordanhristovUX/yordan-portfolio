/* GENERATED by scripts/build.mjs from components/entry/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/entry (dist/react/entry.tsx).
   The element comes from the canonical HTML in components/entry/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.entry`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const entry = cva([
  "grid",
  "[grid-template-columns:1fr_minmax(9rem,_14rem)]",
  "gap-y-space-2",
  "gap-x-space-7",
  "py-space-flow",
  "px-pad",
  "[border-bottom:var(--rule)]",
  "[&:last-child]:[border-bottom:0]",
  /* Under `@media (max-width: 720px)` (`below-720`) — One column. The dates stop being an
     edge to scan and become a line of the record like any other, so the span leaves its own
     column, gives up its row span and aligns left with everything else. The gap collapses
     to the smallest step because a stacked record is a paragraph rather than a table. */
  "[@media(max-width:720px)]:[grid-template-columns:1fr]",
  "[@media(max-width:720px)]:gap-space-1",
]);

export type EntryVariants = VariantProps<typeof entry>;

export type EntryProps = EntryVariants & { className?: string } & Omit<ComponentPropsWithRef<"article">, "className">;

/**
 * Renders the canonical pattern of components/entry/spec.md: a <article>. Styling only —
 * behaviour is the consumer's.
 */
export function Entry({ className, ...rest }: EntryProps) {
  return <article className={cx(entry(), className)} {...rest} />;
}

/**
 * What the person was. Display family at the sub step, uppercase — the same voice a case
 * study's section heading uses, one level down from a section title.
 */
export const entryRole = cva([
  "font-display",
  "font-extrabold",
  "[font-variation-settings:var(--width-sub)]",
  "text-step-sub",
  "[line-height:1.1]",
  "uppercase",
  "text-content-primary",
]);

export type EntryRoleProps = { className?: string } & Omit<ComponentPropsWithRef<"h3">, "className">;

/** The `.entry__role` half of the pattern in components/entry/spec.md: a <h3>. */
export function EntryRole({ className, ...rest }: EntryRoleProps) {
  return <h3 className={cx(entryRole(), className)} {...rest} />;
}

/**
 * Where. Body weight lifted to the primary ink, so the role and the organisation read as one
 * line of two weights rather than as a heading and a caption.
 */
export const entryOrg = cva([
  "font-semibold",
  "text-content-primary",
  /* `.entry__org em` — The qualifier inside the organisation — a contract, a division, a
     former name. It steps back to muted body weight, which is the only place in this block
     where emphasis markup means de-emphasis: <em> is the semantics the content pipeline has
     for "this is an aside", and the styling follows the meaning rather than the tag's
     default. */
  "[&_em]:text-content-muted",
  "[&_em]:font-regular",
]);

export type EntryOrgProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.entry__org` half of the pattern in components/entry/spec.md: a <p>. */
export function EntryOrg({ className, ...rest }: EntryOrgProps) {
  return <p className={cx(entryOrg(), className)} {...rest} />;
}

/**
 * The dates. Mono, uppercase and right-aligned so that a column of them forms the readable
 * edge the banner describes: you can scan the years without reading the roles.
 */
export const entrySpan = cva([
  "[grid-column:2]",
  "[grid-row:1_/_span_2]",
  "font-mono",
  "text-step-xs",
  "tracking-step-wide",
  "text-chrome-label",
  "text-right",
  "uppercase",
  /* Under `@media (max-width: 720px)` (`below-720`) — One column. The dates stop being an
     edge to scan and become a line of the record like any other, so the span leaves its own
     column, gives up its row span and aligns left with everything else. The gap collapses
     to the smallest step because a stacked record is a paragraph rather than a table. */
  "[@media(max-width:720px)]:[grid-column:1]",
  "[@media(max-width:720px)]:[grid-row:auto]",
  "[@media(max-width:720px)]:text-left",
]);

export type EntrySpanProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.entry__span` half of the pattern in components/entry/spec.md: a <p>. */
export function EntrySpan({ className, ...rest }: EntrySpanProps) {
  return <p className={cx(entrySpan(), className)} {...rest} />;
}

/** One sentence of context under the org, in column 1 so it does not run under the dates. */
export const entryLede = cva([
  "[grid-column:1]",
  "text-content-muted",
  "text-step-md",
]);

export type EntryLedeProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.entry__lede` half of the pattern in components/entry/spec.md: a <p>. */
export function EntryLede({ className, ...rest }: EntryLedeProps) {
  return <p className={cx(entryLede(), className)} {...rest} />;
}

/**
 * What happened, across both columns — the list is the widest thing in the record and has no
 * reason to stop at the date edge.
 */
export const entryList = cva([
  "[grid-column:1_/_-1]",
  "[list-style:none]",
  "[margin-top:var(--space-3)]",
  /* `.entry__list li` — One thing that happened. `position: relative` is here for the
     marker below it; the left padding is the space that marker sits in. The measure is
     wider than a case study's because these are single lines rather than paragraphs. */
  "[&_li]:[position:relative]",
  "[&_li]:pt-space-2",
  "[&_li]:pr-0",
  "[&_li]:pb-space-2",
  "[&_li]:pl-space-6",
  "[&_li]:text-step-md",
  "[&_li]:[line-height:1.55]",
  "[&_li]:[max-width:52rem]",
  /* `.entry__list li::before` — The same list mark case-body draws, for the same reason:
     ::before rather than a character in the text, so it is decoration and is not read out.
     The two blocks state it twice because they are two blocks — a shared rule would be a
     third selector belonging to neither. */
  "[&_li::before]:[content:'▪']",
  "[&_li::before]:[position:absolute]",
  "[&_li::before]:[left:0]",
  "[&_li::before]:[top:var(--space-2)]",
  "[&_li::before]:text-primary",
  "[&_li::before]:text-step-2xs",
  /* `.entry__list strong` — Lifts to the primary ink inside a list item. Scoped to the list
     rather than to the item, which is what the stylesheet says — a <strong> anywhere in the
     list gets it, not only one directly inside an <li>. */
  "[&_strong]:text-content-primary",
  "[&_strong]:font-semibold",
]);

export type EntryListProps = { className?: string } & Omit<ComponentPropsWithRef<"ul">, "className">;

/** The `.entry__list` half of the pattern in components/entry/spec.md: a <ul>. */
export function EntryList({ className, ...rest }: EntryListProps) {
  return <ul className={cx(entryList(), className)} {...rest} />;
}
