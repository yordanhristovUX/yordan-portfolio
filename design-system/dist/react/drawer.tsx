/* GENERATED by scripts/build.mjs from components/drawer/definition.json — do not edit.
   Edit the definition and run `npm run build`; `build.mjs --check` byte-compares this file,
   so a hand-edit fails the build rather than being silently overwritten.

   Published as @yordan/design-system/react/drawer (dist/react/drawer.tsx).
   The element comes from the canonical HTML in components/drawer/spec.md, which
   design-system/README.md calls THE pattern rather than an example.

   STYLING IS DATA, BEHAVIOUR IS CODE. There is no state, no effect, no event handler and
   no ARIA here, and none is coming — those are the consumer's, written by hand. This file
   is a class map, a type and an element.

   REQUIRES, from the consumer: tailwindcss v4 with @yordan/design-system/tokens.css and
   tokens.tailwind.css imported and this directory named in an @source; the peer packages
   class-variance-authority and react; and a build that transpiles TSX out of node_modules
   (Next: `transpilePackages: ["@yordan/design-system"]`). This package declares no
   dependencies of its own and installs nothing. */
import { cva, cx, type VariantProps } from "class-variance-authority";
import type { ComponentPropsWithRef } from "react";

/**
 * The class map for `.drawer`, rendered from its definition. The base carries only what no
 * variant or size overrides; anything one of them does override sits in that axis's `default`
 * branch instead, so exactly one of the two classes is ever in the attribute.
 */
export const drawer = cva([
  "[position:fixed]",
  "[inset:0]",
  "[z-index:400]",
  "[visibility:hidden]",
  "[transition:visibility_0s_linear_0.28s]",
  "[&[data-open]]:[visibility:visible]",
  "[&[data-open]]:[transition-delay:0s]",
  /* `.drawer[data-open] .drawer__scrim` */
  "[&[data-open]_.drawer__scrim]:[opacity:1]",
  /* `.drawer[data-open] .drawer__sheet` */
  "[&[data-open]_.drawer__sheet]:[transform:none]",
  /* `.drawer .chat` */
  "[&_.chat]:[min-height:0]",
  "[&_.chat]:[min-width:0]",
  "[&_.chat]:[grid-template-rows:minmax(0,_1fr)]",
  "[&_.chat]:[grid-auto-rows:auto]",
  /* `.drawer .chat__thread` */
  "[&_.chat__thread]:[max-height:none]",
  "[&_.chat__thread]:[min-height:0]",
  /* `.drawer .chat__thread:empty` */
  "[&_.chat__thread:empty]:grid",
  "[&_.chat__thread:empty]:[border-top:0]",
  /* `.drawer .idx__row` */
  "[&_.idx__row]:[grid-template-columns:minmax(0,_1fr)_auto]",
  /* `.drawer .idx__tags` */
  "[&_.idx__tags]:hidden",
  /* `.drawer .entry` */
  "[&_.entry]:[grid-template-columns:1fr]",
  "[&_.entry]:gap-space-1",
  /* `.drawer .entry__span` */
  "[&_.entry__span]:[grid-column:1]",
  "[&_.entry__span]:[grid-row:auto]",
  "[&_.entry__span]:text-left",
  /* `.drawer .profile` */
  "[&_.profile]:[grid-template-columns:1fr]",
  /* `.drawer .profile > div` — A SCOPE, NOT AN APPEARANCE — the part that has no
     declarations and is the block that made them optional. The stylesheet writes `.drawer
     .profile > div:nth-child(odd)` and never `.drawer .profile > div`, because the only
     thing the drawer says about a hosted pair is what it says about the LEFT-hand ones: the
     gutter between two columns has nothing to be between once the grid is one column.
     Emitting an empty rule for the wrapper would claim a decision that was never made. */
  "[&_.profile>div:nth-child(odd)]:[padding-right:0]",
  /* Under `@media (max-width: 560px)` (`below-560`) */
  "[@media(max-width:560px)]:[&_.idx__row]:[grid-template-columns:1fr]",
]);

export type DrawerVariants = VariantProps<typeof drawer>;

export type DrawerProps = DrawerVariants & { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/**
 * Renders the canonical pattern of components/drawer/spec.md: a <div>. Styling only —
 * behaviour is the consumer's.
 */
export function Drawer({ className, ...rest }: DrawerProps) {
  return <div className={cx(drawer(), className)} {...rest} />;
}

export const drawerScrim = cva([
  "[position:absolute]",
  "[inset:0]",
  "bg-scrim",
  "[opacity:0]",
  "[transition:opacity_0.28s_ease]",
]);

export type DrawerScrimProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.drawer__scrim` half of the pattern in components/drawer/spec.md: a <div>. */
export function DrawerScrim({ className, ...rest }: DrawerScrimProps) {
  return <div className={cx(drawerScrim(), className)} {...rest} />;
}

export const drawerSheet = cva([
  "[position:absolute]",
  "[top:0]",
  "[right:0]",
  "[bottom:0]",
  "[width:min(46rem,_100%)]",
  "grid",
  "[grid-template-rows:auto_minmax(0,_1fr)]",
  "[grid-template-columns:minmax(0,_1fr)]",
  "bg-surface-page",
  "[box-shadow:inset_1px_0_0_var(--chrome-border-strong)]",
  "[transform:translateX(100%)]",
  "[transition:transform_0.28s_cubic-bezier(0.22,_1,_0.36,_1)]",
  "[will-change:transform]",
  /* Under `@media (max-width: 1024px)` (`below-1024`) */
  "[@media(max-width:1024px)]:[width:100%]",
]);

export type DrawerSheetProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.drawer__sheet` half of the pattern in components/drawer/spec.md: a <div>. */
export function DrawerSheet({ className, ...rest }: DrawerSheetProps) {
  return <div className={cx(drawerSheet(), className)} {...rest} />;
}

export const drawerHead = cva([
  "grid",
  "[grid-template-columns:auto_minmax(0,_1fr)_auto]",
  "[align-items:center]",
  "gap-space-4",
  "py-space-3",
  "px-space-5",
  "bg-chrome-bg",
  "[border-bottom:var(--rule-strong)]",
]);

export type DrawerHeadProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.drawer__head` half of the pattern in components/drawer/spec.md: a <div>. */
export function DrawerHead({ className, ...rest }: DrawerHeadProps) {
  return <div className={cx(drawerHead(), className)} {...rest} />;
}

export const drawerPortrait = cva([
  "block",
  "[width:3.5rem]",
  "[height:3.5rem]",
  "[border-radius:50%]",
  "[box-shadow:inset_0_0_0_1px_var(--chrome-border-strong)]",
  /* `.drawer__portrait img` */
  "[&_img]:block",
  "[&_img]:[width:100%]",
  "[&_img]:[height:100%]",
]);

export type DrawerPortraitProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.drawer__portrait` half of the pattern in components/drawer/spec.md: a <span>. */
export function DrawerPortrait({ className, ...rest }: DrawerPortraitProps) {
  return <span className={cx(drawerPortrait(), className)} {...rest} />;
}

export const drawerHeading = cva([
  "[min-width:0]",
]);

export type DrawerHeadingProps = { className?: string } & Omit<ComponentPropsWithRef<"span">, "className">;

/** The `.drawer__heading` half of the pattern in components/drawer/spec.md: a <span>. */
export function DrawerHeading({ className, ...rest }: DrawerHeadingProps) {
  return <span className={cx(drawerHeading(), className)} {...rest} />;
}

export const drawerTitle = cva([
  "font-display",
  "font-extrabold",
  "[font-variation-settings:var(--width-title)]",
  "text-step-sub",
  "[line-height:1]",
  "uppercase",
  "tracking-step-tight",
  "text-content-primary",
]);

export type DrawerTitleProps = { className?: string } & Omit<ComponentPropsWithRef<"h2">, "className">;

/** The `.drawer__title` half of the pattern in components/drawer/spec.md: a <h2>. */
export function DrawerTitle({ className, ...rest }: DrawerTitleProps) {
  return <h2 className={cx(drawerTitle(), className)} {...rest} />;
}

export const drawerSubtitle = cva([
  "[margin-top:var(--space-1)]",
  "font-mono",
  "text-step-2xs",
  "tracking-step-wide",
  "text-chrome-label",
  /* Under `@media (max-width: 560px)` (`below-560`) */
  "[@media(max-width:560px)]:hidden",
]);

export type DrawerSubtitleProps = { className?: string } & Omit<ComponentPropsWithRef<"p">, "className">;

/** The `.drawer__note` half of the pattern in components/drawer/spec.md: a <p>. */
export function DrawerSubtitle({ className, ...rest }: DrawerSubtitleProps) {
  return <p className={cx(drawerSubtitle(), className)} {...rest} />;
}

export const drawerBody = cva([
  "[min-height:0]",
  "[min-width:0]",
  "grid",
  "[grid-template-columns:minmax(0,_1fr)]",
  "[overflow:hidden]",
  "p-space-5",
]);

export type DrawerBodyProps = { className?: string } & Omit<ComponentPropsWithRef<"div">, "className">;

/** The `.drawer__body` half of the pattern in components/drawer/spec.md: a <div>. */
export function DrawerBody({ className, ...rest }: DrawerBodyProps) {
  return <div className={cx(drawerBody(), className)} {...rest} />;
}
