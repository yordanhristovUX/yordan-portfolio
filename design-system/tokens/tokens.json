{
  "$meta": {
    "name": "Blueprint portfolio design tokens",
    "description": "Single source of truth. Edit THIS file, never dist/. Run `npm run build` (or `node scripts/build.mjs`) to regenerate dist/tokens.css (consumed by the site) and dist/tokens.flat.json (consumed by AI agents and the Figma push procedure).",
    "architecture": "Two tiers: raw palettes (stone = warm content field, slate = cold chrome, ink = primary) and a semantic layer that aliases them via var() references. Components may ONLY use semantic tokens; raw ramp values never appear outside this file.",
    "themes": "A token may carry a `dark` value beside its light one: { \"value\": <light>, \"dark\": <dark> }. Only the semantic tier does — the raw ramps are theme-independent, and dark mode re-aliases them rather than restating colours. The build emits those overrides twice: under `@media (prefers-color-scheme: dark)` for visitors who never touch the control, and under `:root[data-theme=\"dark\"]` for those who pin it. Components stay theme-agnostic: if a component needs a conditional colour, the condition belongs in a token here, never in a media query in components.css.",
    "print": "A token may also carry a `print` value, emitted last as `@media print` so it beats both screen themes. Paper is a third theme with physical constraints rather than preferences: no backlight, ink costs money, and greyscale is likely — so surfaces collapse to one, the accent returns to ink, and shadows and the skeleton grid disappear entirely. Print values alias the raw ramps like any other token; nothing here is a new literal. Print is not only a colour tier: the `text` scale and `space-nav` carry print values too, because paper needs a different type size (pt, not rem) and has no viewport for a `vw` clamp to scale against.",
    "size": "Type and rhythm are tokens on exactly the same terms as colour: `scripts/check-css.mjs` fails the build on any literal `font-size` in components.css or a page stylesheet. Before this file grew a `text` category the site carried 89 font-size declarations at 40 distinct values, fourteen of them inside a single 0.34rem band — differences no reader can perceive and no system justifies."
  },
  "color-stone": {
    "$doc": "Raw: Tailwind stone (warm) — the paper and the prose",
    "stone-50": "#fafaf9",
    "stone-100": "#f5f5f4",
    "stone-200": "#e7e5e4",
    "stone-300": "#d6d3d1",
    "stone-400": "#a8a29e",
    "stone-500": "#78716c",
    "stone-600": "#57534e",
    "stone-700": "#44403c",
    "stone-800": "#292524",
    "stone-900": "#1c1917",
    "stone-950": "#0c0a09"
  },
  "color-slate": {
    "$doc": "Raw: Tailwind slate (cold) — the machine's furniture",
    "slate-50": "#f8fafc",
    "slate-100": "#f1f5f9",
    "slate-200": "#e2e8f0",
    "slate-300": "#cbd5e1",
    "slate-400": "#94a3b8",
    "slate-500": "#64748b",
    "slate-600": "#475569",
    "slate-700": "#334155",
    "slate-800": "#1e293b",
    "slate-900": "#0f172a",
    "slate-950": "#020617"
  },
  "color-ink": {
    "$doc": "Raw: ink (iron-gall, warm-black with a cold undertone)",
    "ink-500": "#3b3e44",
    "ink-700": "#24262b",
    "ink-900": "#141518"
  },
  "scheme": {
    "$doc": "Tells the browser which built-in palette to use for scrollbars, form controls and the canvas. Applied once, in components.css, as `color-scheme: var(--color-scheme)`.",
    "color-scheme": { "value": "light", "dark": "dark", "print": "light" }
  },
  "surface": {
    "$doc": "Semantic surfaces (warm — the paper). Dark mode does not tint the paper blue: it walks the same stone ramp to the far end, so the sheet stays warm and the chrome stays cold in both themes. On paper every surface collapses to one: printers do not render a page background, and tinted blocks only cost ink.",
    "surface-page": { "value": "var(--stone-100)", "dark": "var(--stone-900)", "print": "var(--stone-50)" },
    "surface-raised": { "value": "var(--stone-50)", "dark": "var(--stone-800)", "print": "var(--stone-50)" },
    "surface-sunken": { "value": "var(--stone-200)", "dark": "var(--stone-950)", "print": "var(--stone-50)" },
    "surface-portrait": {
      "value": "var(--stone-50)",
      "dark": "var(--stone-100)",
      "description": "The plate the face illustration is printed on, and the ONE surface that does not walk the ramp to the far end in dark. The portrait is dark line-art over light fills (its densest fill is #DDDCDE), so on a stone-900 page it loses its hair and its jacket and leaves a face floating in the dark — measured, not assumed. Kept light in both themes it reads as a printed card lying on the page, which is the same 'object on the sheet' language as the nav bar's drop shadow, and it avoids the alternative everyone reaches for first: a CSS invert() filter, which makes an uncanny negative of a human face and flips his blue collar to orange. It is stone-100 rather than stone-50 in dark because at 3.5rem on near-black the brightest paper in the ramp is a light source rather than a card; stone-100 is the page's own light-theme paper, which is exactly the fiction being maintained. No print value: the drawer that carries it is display:none on paper."
    }
  },
  "primary": {
    "$doc": "The ONE changeable brand colour. Everything that reads as 'brand' (headline type, interactive states, the automata's resident ink) routes through these three. Retheme by editing them alone. In dark mode the ink inverts to paper: primary is what the page is NOT, which is why the same components read correctly with no per-component overrides.",
    "primary": { "value": "var(--ink-900)", "dark": "var(--stone-50)", "print": "var(--ink-900)" },
    "primary-hover": { "value": "var(--ink-700)", "dark": "var(--stone-300)", "print": "var(--ink-700)" },
    "primary-muted": { "value": "var(--ink-500)", "dark": "var(--stone-400)", "print": "var(--ink-500)" }
  },
  "content": {
    "$doc": "Semantic content colours. content-muted is stone-600, not -500: -500 measures 4.4:1 on the page background, just under the WCAG AA text minimum (4.5:1); -600 clears it (6.99:1). Dark mirrors the pairing — stone-400 on stone-900 measures 6.94:1, the same relationship read from the other end of the ramp.",
    "content-primary": "var(--primary)",
    "content-body": { "value": "var(--stone-700)", "dark": "var(--stone-300)", "print": "var(--stone-800)" },
    "content-muted": { "value": "var(--stone-600)", "dark": "var(--stone-400)", "print": "var(--stone-600)" },
    "content-inverse": { "value": "var(--stone-100)", "dark": "var(--stone-900)", "print": "var(--stone-100)" }
  },
  "chrome": {
    "$doc": "Semantic chrome (cold). chrome-label is slate-600, not -500, for the same AA-contrast reason.",
    "chrome-bg": { "value": "var(--slate-100)", "dark": "var(--slate-900)", "print": "var(--stone-50)" },
    "chrome-bg-strong": { "value": "var(--slate-900)", "dark": "var(--slate-950)", "print": "var(--stone-50)" },
    "chrome-border": { "value": "var(--slate-300)", "dark": "var(--slate-700)", "print": "var(--stone-300)" },
    "chrome-border-strong": { "value": "var(--slate-400)", "dark": "var(--slate-600)", "print": "var(--stone-400)" },
    "chrome-label": { "value": "var(--slate-600)", "dark": "var(--slate-400)", "print": "var(--stone-600)" },
    "chrome-label-strong": { "value": "var(--slate-700)", "dark": "var(--slate-300)", "print": "var(--stone-700)" },
    "chrome-label-on-strong": {
      "value": "var(--slate-400)",
      "print": "var(--stone-700)",
      "description": "Text sitting on chrome-bg-strong (the footer). Has no dark variant on purpose: that surface is near-black in BOTH themes, so its label must stay light in both. 6.96:1 on slate-900, 7.87:1 on slate-950. It DOES need a print value: on paper that surface turns white, so a light label would vanish."
    },
    "chrome-grid": {
      "value": "rgba(100, 116, 139, 0.16)",
      "dark": "rgba(148, 163, 184, 0.14)",
      "print": "transparent",
      "description": "The skeleton's square lines — slate-500 at 16% on paper, slate-400 at 14% on dark (a lighter ink at lower alpha, so the grid stays a whisper rather than becoming a cage). Prints as nothing: the grid is screen scaffolding, not content."
    }
  },
  "action": {
    "$doc": "Interactive states — always the primary ink. `action` itself was deleted: it aliased `primary` verbatim, nothing consumed it, and a semantic tier earns its keep by being consumed. `action-hover` stays because `.btn--solid:hover` reads it and the distinction (hover is a state, not the brand) is real.",
    "action-hover": "var(--primary-hover)"
  },
  "accent": {
    "$doc": "Blueprint blue, used scarcely. Only five places wear it: role underline, section numbers, availability, the square cursor + user-seeded life, focus rings. The dark value is the same hue lifted to 68% lightness — 47% blue on a stone-900 ground measures 1.9:1, invisible; 68% measures 5.19:1 on the page and 5.30:1 on the chrome, so the accent keeps its AA rating in both themes without changing hue.",
    "accent": {
      "value": "hsl(225 100% 47%)",
      "dark": "hsl(225 100% 68%)",
      "print": "var(--ink-900)",
      "description": "Prints as ink, not blue. A CV is as likely to be printed greyscale as in colour, and blue reduced to mid-grey reads as a mistake rather than an accent."
    },
    "accent-rgb": {
      "value": "0, 60, 240",
      "dark": "92, 133, 255",
      "description": "rgb triplet of --accent, for rgba() composition in JS and hover states"
    }
  },
  "effect": {
    "$doc": "Composited colours that are neither surface nor content — they sit BETWEEN layers, so they cannot be aliased to a ramp step and are written literally here (the one file where that is allowed).",
    "shadow-drop": {
      "value": "rgba(15, 23, 42, 0.15)",
      "dark": "rgba(2, 6, 23, 0.55)",
      "print": "transparent",
      "description": "The hard offset shadow under the floating nav — an object lying on the sheet. Deeper and more opaque in dark, where a 15% veil would vanish. Nothing on paper: a shadow printed is just a grey smear."
    },
    "scrim": {
      "value": "rgba(28, 25, 23, 0.55)",
      "dark": "rgba(2, 6, 23, 0.72)",
      "print": "transparent",
      "description": "The veil behind the case-study dialog. Warm stone in light (it dims paper), cold slate in dark (it dims chrome)."
    }
  },
  "automata": {
    "$doc": "The Squares engine (site js/automata.js) reads these at runtime via getComputedStyle and recomposes them into rgba() per cell — which is why they are rgb triplets, not colours. Living cells are deliberately NOT primary: the resident population is a mid-ramp grey that recedes, so the automata never competes with type.",
    "automata-cell-rgb": {
      "value": "120, 113, 108",
      "dark": "168, 162, 158",
      "description": "The resident population — stone-500 on paper, stone-400 on dark (the ramp inverts with the surface, or the cells would sink into the page)"
    }
  },
  "font": {
    "$doc": "The humanist-machine family: Archivo for display, IBM Plex for body and mono",
    "font-display": "\"Archivo\", \"Arial Black\", sans-serif",
    "font-body": "\"IBM Plex Sans\", system-ui, sans-serif",
    "font-mono": "\"IBM Plex Mono\", ui-monospace, monospace"
  },
  "text": {
    "$doc": "The type scale: one ramp of twelve steps, static up to body and fluid above it. That split is the whole idea. Below body a size has no voice — it is chrome, and chrome must not resize with the window or a 12px label becomes an 18px one on a wide monitor; so those four steps and body itself are fixed rem on a ~1.085 ratio. Above body every step IS a voice in the hierarchy (lead, sub, heading, title, display), and a display line has to survive both a 375px phone and a 1600px sheet, so those seven are `clamp(min, vw, max)`. Every step also carries a `print` value in pt: paper is a third theme for type exactly as it is for colour, and a `vw` clamp has nothing to scale against on a page. The two `em` entries at the end are ratios, not steps — they mean 'a fraction of whatever set me', which is a relationship the scale cannot express.",
    "text-2xs": { "value": "0.72rem", "print": "7pt", "description": "The floor. Chips, the tool trace, the source list's ordinals, the ▪ bullets — text you are not meant to read so much as recognise." },
    "text-xs": { "value": "0.78rem", "print": "7.5pt", "description": "The standard chrome label: the nav bar, buttons, section numbers, dated spans, page notes. Twelve of the old 89 declarations were already this value — it was the de-facto step, now it is the actual one." },
    "text-sm": { "value": "0.85rem", "print": "8pt", "description": "Small prose — captions, fact labels, citation lines, code blocks." },
    "text-md": { "value": "0.92rem", "print": "9pt", "description": "Secondary prose: card copy, list items, definition bodies. Body's quieter sibling." },
    "text-base": { "value": "1rem", "print": "9.5pt", "description": "Body. Set on `body` in components.css; everything else is measured from here." },
    "text-lead": { "value": "clamp(1.1rem, 1.7vw, 1.35rem)", "print": "9.5pt", "description": "The reading lede — one step above body, the paragraph that introduces a section. On paper it collapses back into body: a CV has no room for an emphasised opening size." },
    "text-sub": { "value": "clamp(1.15rem, 2vw, 1.5rem)", "print": "10pt", "description": "Sub-titles: card titles, a role in an entry, an <h3> inside a case study. Three near-identical ramps used to live here — clamp(1.15rem,1.7vw,1.45rem), clamp(1.1rem,1.9vw,1.45rem) and clamp(1.2rem,2.2vw,1.55rem) — rendering at 1280px as 21.76 / 23.2 / 24.8px. That is drift, not hierarchy. A fourth, the work index's project name, was a genuine step larger and became `text-heading`." },
    "text-heading": { "value": "clamp(1.3rem, 2.4vw, 1.9rem)", "print": "10.5pt", "description": "One step above a sub-title and below a section title: the work index's project names, which are the largest thing in a scannable list and must not fall back to card-title size." },
    "text-title": { "value": "clamp(1.65rem, 3.7vw, 2.8rem)", "print": "11pt", "description": "Section titles and the one statement per section. `.t-title` and `.t-statement` were 2.9rem and 2.7rem — the same level wearing different weights and cases, which is what the utility classes are for." },
    "text-display": { "value": "clamp(2.1rem, 4.8vw, 3.45rem)", "print": "20pt", "description": "Display: dialog titles, the CV name, a stat, a fact's number. `.fact__num` was clamp(2.2rem, 4.5vw, 3.5rem) and `.stat` was clamp(2.2rem, 5vw, 3.5rem) — a 0.5vw difference nobody chose." },
    "text-display-xl": { "value": "clamp(2.6rem, 8.5vw, 7.5rem)", "print": "26pt", "description": "The contact headline." },
    "text-display-hero": { "value": "clamp(3rem, 11.5vw, 10.5rem)", "print": "32pt", "description": "The hero name, once per site." },
    "text-code": { "value": "0.9em", "description": "Inline <code> inside prose. A RATIO, not a step: monospace runs optically larger than the sans beside it, so it is set relative to whatever paragraph it lands in." },
    "text-unit": { "value": "0.38em", "description": "The unit suffix riding on a display number (`+`, `%`, `×`). A ratio for the same reason: it must stay proportional to the number it annotates, whatever size that number lands at." }
  },
  "space": {
    "$doc": "Rhythm, in two halves that answer different questions. `space-1..7` is a fixed 4px-based ramp (4/8/12/16/20/24/32) for space INSIDE a component — a chip's padding does not want to know how tall the window is. The fluid tokens are for space BETWEEN things, where the page's own rhythm should breathe with the viewport: `pad`/`pad-y` are the content well's inset, `space-flow*` are the vertical steps between blocks, and `space-nav` is the one clearance every page head needs under the floating bar. `space-nav` is the only spacing token with a `print` value, and it is 0 — the bar it clears does not exist on paper, and the same three page stylesheets each used to reset it by hand.",
    "space-1": "0.25rem",
    "space-2": "0.5rem",
    "space-3": "0.75rem",
    "space-4": "1rem",
    "space-5": "1.25rem",
    "space-6": "1.5rem",
    "space-7": "2rem",
    "space-flow-sm": "clamp(1rem, 2.5vh, 1.5rem)",
    "space-flow": "clamp(1.25rem, 3.5vh, 2rem)",
    "space-flow-lg": "clamp(2rem, 5vh, 3rem)",
    "space-nav": {
      "value": "clamp(5.5rem, 14vh, 8rem)",
      "print": "0",
      "description": "Clearance under the fixed nav bar, on every page head. Prints as nothing: `.bar` is display:none on paper, so the clearance would be 88px of blank at the top of the sheet."
    },
    "pad": "clamp(1.25rem, 3vw, 3rem)",
    "pad-y": "clamp(2rem, 5vh, 3.5rem)"
  },
  "border": {
    "$doc": "Rule shorthands. Used as border/box-shadow values in chrome; the square skeleton itself never uses border (inset shadows keep fr-track math exact).",
    "rule": "1px solid var(--chrome-border)",
    "rule-strong": {
      "value": "2px solid var(--content-primary)",
      "dark": "2px solid var(--chrome-border-strong)",
      "print": "2px solid var(--ink-900)",
      "description": "The heavy rule. In light it is drawn ink — the section signature. Inverting it literally would make it a 2px stone-50 bar glaring across every section top, so on dark it becomes the strong chrome border instead: still structural, no longer a light source. On paper it returns to ink — the one place the rule is cheap and does the most work."
    }
  }
}
