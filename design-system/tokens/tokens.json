{
  "$meta": {
    "name": "Blueprint portfolio design tokens",
    "description": "Single source of truth. Edit THIS file, never dist/. Run `npm run build` (or `node scripts/build.mjs`) to regenerate dist/tokens.css (consumed by the site) and dist/tokens.flat.json (consumed by AI agents and the Figma push procedure).",
    "architecture": "Two tiers: raw palettes (stone = warm content field, slate = cold chrome, ink = primary) and a semantic layer that aliases them via var() references. Components may ONLY use semantic tokens; raw ramp values never appear outside this file.",
    "themes": "A token may carry a `dark` value beside its light one: { \"value\": <light>, \"dark\": <dark> }. Only the semantic tier does — the raw ramps are theme-independent, and dark mode re-aliases them rather than restating colours. The build emits those overrides twice: under `@media (prefers-color-scheme: dark)` for visitors who never touch the control, and under `:root[data-theme=\"dark\"]` for those who pin it. Components stay theme-agnostic: if a component needs a conditional colour, the condition belongs in a token here, never in a media query in components.css.",
    "print": "A token may also carry a `print` value, emitted last as `@media print` so it beats both screen themes. Paper is a third theme with physical constraints rather than preferences: no backlight, ink costs money, and greyscale is likely — so surfaces collapse to one, the accent returns to ink, and shadows and the skeleton grid disappear entirely. Print values alias the raw ramps like any other token; nothing here is a new literal."
  },
  "color-stone": {
    "$doc": "Raw: Tailwind stone (warm) — the paper and the prose",
    "stone-50": "#fafaf9",
    "stone-100": "#f5f5f4",
    "stone-200": "#e7e5e4",
    "stone-300": "#d6d3d1",
    "stone-400": "#a8a29e",
    "stone-500": "#78716c",
    "stone-600": "#57534e",
    "stone-700": "#44403c",
    "stone-800": "#292524",
    "stone-900": "#1c1917",
    "stone-950": "#0c0a09"
  },
  "color-slate": {
    "$doc": "Raw: Tailwind slate (cold) — the machine's furniture",
    "slate-50": "#f8fafc",
    "slate-100": "#f1f5f9",
    "slate-200": "#e2e8f0",
    "slate-300": "#cbd5e1",
    "slate-400": "#94a3b8",
    "slate-500": "#64748b",
    "slate-600": "#475569",
    "slate-700": "#334155",
    "slate-800": "#1e293b",
    "slate-900": "#0f172a",
    "slate-950": "#020617"
  },
  "color-ink": {
    "$doc": "Raw: ink (iron-gall, warm-black with a cold undertone)",
    "ink-500": "#3b3e44",
    "ink-700": "#24262b",
    "ink-900": "#141518"
  },
  "scheme": {
    "$doc": "Tells the browser which built-in palette to use for scrollbars, form controls and the canvas. Applied once, in components.css, as `color-scheme: var(--color-scheme)`.",
    "color-scheme": { "value": "light", "dark": "dark", "print": "light" }
  },
  "surface": {
    "$doc": "Semantic surfaces (warm — the paper). Dark mode does not tint the paper blue: it walks the same stone ramp to the far end, so the sheet stays warm and the chrome stays cold in both themes. On paper every surface collapses to one: printers do not render a page background, and tinted blocks only cost ink.",
    "surface-page": { "value": "var(--stone-100)", "dark": "var(--stone-900)", "print": "var(--stone-50)" },
    "surface-raised": { "value": "var(--stone-50)", "dark": "var(--stone-800)", "print": "var(--stone-50)" },
    "surface-sunken": { "value": "var(--stone-200)", "dark": "var(--stone-950)", "print": "var(--stone-50)" },
    "surface-inverse": { "value": "var(--stone-900)", "dark": "var(--stone-100)", "print": "var(--stone-900)" }
  },
  "primary": {
    "$doc": "The ONE changeable brand colour. Everything that reads as 'brand' (headline type, interactive states, the automata's resident ink) routes through these three. Retheme by editing them alone. In dark mode the ink inverts to paper: primary is what the page is NOT, which is why the same components read correctly with no per-component overrides.",
    "primary": { "value": "var(--ink-900)", "dark": "var(--stone-50)", "print": "var(--ink-900)" },
    "primary-hover": { "value": "var(--ink-700)", "dark": "var(--stone-300)", "print": "var(--ink-700)" },
    "primary-muted": { "value": "var(--ink-500)", "dark": "var(--stone-400)", "print": "var(--ink-500)" }
  },
  "content": {
    "$doc": "Semantic content colours. content-muted is stone-600, not -500: -500 measures 4.4:1 on the page background, just under the WCAG AA text minimum (4.5:1); -600 clears it (6.99:1). Dark mirrors the pairing — stone-400 on stone-900 measures 6.94:1, the same relationship read from the other end of the ramp.",
    "content-primary": "var(--primary)",
    "content-body": { "value": "var(--stone-700)", "dark": "var(--stone-300)", "print": "var(--stone-800)" },
    "content-muted": { "value": "var(--stone-600)", "dark": "var(--stone-400)", "print": "var(--stone-600)" },
    "content-inverse": { "value": "var(--stone-100)", "dark": "var(--stone-900)", "print": "var(--stone-100)" }
  },
  "chrome": {
    "$doc": "Semantic chrome (cold). chrome-label is slate-600, not -500, for the same AA-contrast reason.",
    "chrome-bg": { "value": "var(--slate-100)", "dark": "var(--slate-900)", "print": "var(--stone-50)" },
    "chrome-bg-strong": { "value": "var(--slate-900)", "dark": "var(--slate-950)", "print": "var(--stone-50)" },
    "chrome-border": { "value": "var(--slate-300)", "dark": "var(--slate-700)", "print": "var(--stone-300)" },
    "chrome-border-strong": { "value": "var(--slate-400)", "dark": "var(--slate-600)", "print": "var(--stone-400)" },
    "chrome-label": { "value": "var(--slate-600)", "dark": "var(--slate-400)", "print": "var(--stone-600)" },
    "chrome-label-strong": { "value": "var(--slate-700)", "dark": "var(--slate-300)", "print": "var(--stone-700)" },
    "chrome-label-on-strong": {
      "value": "var(--slate-400)",
      "print": "var(--stone-700)",
      "description": "Text sitting on chrome-bg-strong (the footer). Has no dark variant on purpose: that surface is near-black in BOTH themes, so its label must stay light in both. 6.96:1 on slate-900, 7.87:1 on slate-950. It DOES need a print value: on paper that surface turns white, so a light label would vanish."
    },
    "chrome-grid": {
      "value": "rgba(100, 116, 139, 0.16)",
      "dark": "rgba(148, 163, 184, 0.14)",
      "print": "transparent",
      "description": "The skeleton's square lines — slate-500 at 16% on paper, slate-400 at 14% on dark (a lighter ink at lower alpha, so the grid stays a whisper rather than becoming a cage). Prints as nothing: the grid is screen scaffolding, not content."
    }
  },
  "action": {
    "$doc": "Interactive states — always the primary ink",
    "action": "var(--primary)",
    "action-hover": "var(--primary-hover)"
  },
  "accent": {
    "$doc": "Blueprint blue, used scarcely. Only five places wear it: role underline, section numbers, availability, the square cursor + user-seeded life, focus rings. The dark value is the same hue lifted to 68% lightness — 47% blue on a stone-900 ground measures 1.9:1, invisible; 68% measures 5.19:1 on the page and 5.30:1 on the chrome, so the accent keeps its AA rating in both themes without changing hue.",
    "accent": {
      "value": "hsl(225 100% 47%)",
      "dark": "hsl(225 100% 68%)",
      "print": "var(--ink-900)",
      "description": "Prints as ink, not blue. A CV is as likely to be printed greyscale as in colour, and blue reduced to mid-grey reads as a mistake rather than an accent."
    },
    "accent-rgb": {
      "value": "0, 60, 240",
      "dark": "92, 133, 255",
      "description": "rgb triplet of --accent, for rgba() composition in JS and hover states"
    }
  },
  "effect": {
    "$doc": "Composited colours that are neither surface nor content — they sit BETWEEN layers, so they cannot be aliased to a ramp step and are written literally here (the one file where that is allowed).",
    "shadow-drop": {
      "value": "rgba(15, 23, 42, 0.15)",
      "dark": "rgba(2, 6, 23, 0.55)",
      "print": "transparent",
      "description": "The hard offset shadow under the floating nav — an object lying on the sheet. Deeper and more opaque in dark, where a 15% veil would vanish. Nothing on paper: a shadow printed is just a grey smear."
    },
    "scrim": {
      "value": "rgba(28, 25, 23, 0.55)",
      "dark": "rgba(2, 6, 23, 0.72)",
      "print": "transparent",
      "description": "The veil behind the case-study dialog. Warm stone in light (it dims paper), cold slate in dark (it dims chrome)."
    }
  },
  "automata": {
    "$doc": "The Squares engine (site js/automata.js) reads these at runtime via getComputedStyle and recomposes them into rgba() per cell — which is why they are rgb triplets, not colours. Living cells are deliberately NOT primary: the resident population is a mid-ramp grey that recedes, so the automata never competes with type.",
    "automata-cell-rgb": {
      "value": "120, 113, 108",
      "dark": "168, 162, 158",
      "description": "The resident population — stone-500 on paper, stone-400 on dark (the ramp inverts with the surface, or the cells would sink into the page)"
    }
  },
  "font": {
    "$doc": "The humanist-machine family: Archivo for display, IBM Plex for body and mono",
    "font-display": "\"Archivo\", \"Arial Black\", sans-serif",
    "font-body": "\"IBM Plex Sans\", system-ui, sans-serif",
    "font-mono": "\"IBM Plex Mono\", ui-monospace, monospace"
  },
  "space": {
    "$doc": "Rhythm — inner padding of the content well",
    "pad": "clamp(1.25rem, 3vw, 3rem)",
    "pad-y": "clamp(2rem, 5vh, 3.5rem)"
  },
  "border": {
    "$doc": "Rule shorthands. Used as border/box-shadow values in chrome; the square skeleton itself never uses border (inset shadows keep fr-track math exact).",
    "rule": "1px solid var(--chrome-border)",
    "rule-strong": {
      "value": "2px solid var(--content-primary)",
      "dark": "2px solid var(--chrome-border-strong)",
      "print": "2px solid var(--ink-900)",
      "description": "The heavy rule. In light it is drawn ink — the section signature. Inverting it literally would make it a 2px stone-50 bar glaring across every section top, so on dark it becomes the strong chrome border instead: still structural, no longer a light source. On paper it returns to ink — the one place the rule is cheap and does the most work."
    }
  }
}
