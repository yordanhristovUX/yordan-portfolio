{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yordanhristov.dev/design-system/definition.schema.json",
  "title": "Component definition",
  "$doc": "EXTRACTED, NOT IMPOSED. Every construct below exists because one of the real definitions needed it, and nothing below exists because a schema felt incomplete without it. Button forced variants, sizes, states-hanging-off-their-owner and the note-on-a-declaration-group; chip forced `parts`, because `.chips` is the wrapper a set of chips sits in and calling it a variant would have cost the word its meaning; stat forced the sections to be OPTIONAL rather than empty, and is the case where a token binds a whole compound value; footer forced `aside`; source made a part a full rule. R4's migration adds two more, and both were forced by blocks whose CSS is nothing BUT the thing the construct expresses: `link-grid` and `case-body` style unclassed markup, which forced a SCOPED part (`within` + `element` + optional `pseudo`), and case-body's first heading forced `positions`, a closed enum rather than a selector. The two constructs that are not a shape but a CLAIM — `aliases` and `axes.orthogonal` — come from findings the pilot reported rather than from the CSS: two rules that are deliberately identical, and two axes that are deliberately a matrix. Both are checked, because an unchecked claim is a comment. WHAT IS NOT HERE: behaviour. No focus management, no keyboard, no ARIA, no state. A definition answers what this looks like and nothing else; the rest lives in spec.md and in code, and this schema refuses the keys that would let it drift. WHAT IS ALSO NOT HERE, and is the harder omission: anything freely relational. There is no at-rule, no locally-defined custom property and no computed geometry, and the one relation that IS here is closed at both ends — a scoped part's ancestor must be the root and its path is bare tag names, so it can say `.case-body p strong` and cannot say `.band > .rail--l`. A block that needs the rest is not expressible here and is not meant to be — see design-system/PATTERNS.md.",
  "type": "object",
  "required": ["id", "block", "root"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string", "$doc": "Optional pointer back to this file, for an editor." },
    "$doc": { "type": "string", "$doc": "Prose about this component's appearance. Deliberately not published: contract-diff.mjs snapshots the resolved declarations and no prose, because rewording a sentence is not a release." },
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "$doc": "Must equal the directory name and the spec.md frontmatter id. Checked by the build, not by this schema, which cannot see the path."
    },
    "block": { "$ref": "#/$defs/block" },
    "root": { "$ref": "#/$defs/selector", "$doc": "The component's own class. Its element is NOT here — it is read from the canonical HTML in spec.md, which design-system/README.md calls THE pattern rather than an example." },
    "axes": { "$ref": "#/$defs/axes" },
    "base": {
      "$ref": "#/$defs/rule",
      "$doc": "OPTIONAL, and `case-body` is the block that made it so. Its root has no rule at all: `.case-body` is a scope rather than an appearance, and everything it does it does to the prose inside it. Requiring `base` would have meant inventing an empty rule and emitting `.case-body { }`, which is a declaration that the file has a shape it does not have. Same move stat forced on `variants`/`sizes` — optional rather than empty. A definition with neither `base` nor `parts` is refused by the build: it would render a banner and nothing under it."
    },
    "variants": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/modifier" },
      "$doc": "Appearance modifiers. An ARRAY, not an object: source order is the cascade in pipeline 1, and JSON object key order is a convention rather than a guarantee. Omitted when there are none — never an empty array."
    },
    "sizes": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/modifier" },
      "$doc": "Dimension modifiers. Separate from `variants` because they are a separate cva axis and a separate React prop, and because the two are usually property-disjoint — which is the claim `axes.orthogonal` makes checkable."
    },
    "parts": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/part" },
      "$doc": "A companion selector that is NOT a modifier of the root. Chip's `.chips` is the whole reason this exists: it is the flex wrapper a group of chips sits in, and no chip knows how many siblings it has."
    }
  },
  "$defs": {
    "block": {
      "$doc": "The CSS banner pipeline 1 emits. `note` is the banner's prose, one array element per emitted line, so the emitter never has to re-wrap somebody's paragraph.",
      "type": "object",
      "required": ["title"],
      "additionalProperties": false,
      "properties": {
        "title": { "type": "string", "pattern": "^[^\\n]+$" },
        "note": { "type": "array", "minItems": 1, "items": { "type": "string", "pattern": "^[^\\n]*$" } }
      }
    },
    "selector": {
      "type": "string",
      "pattern": "^\\.[a-z][a-z0-9_-]*$",
      "$doc": "A single class selector and nothing else. No combinator, no pseudo-class, no element — a definition describes one element's appearance, and the moment it can say `.band > .rail--l` it has become CSS with more punctuation."
    },
    "binding": {
      "$doc": "A reference to a design token: rendered as var(--token) by pipeline 1 and as the utility bound to that token by pipeline 2. The build checks the token exists.",
      "type": "object",
      "required": ["token"],
      "additionalProperties": false,
      "properties": { "token": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" } }
    },
    "term": {
      "$doc": "One term of a value: a structural literal, or a token binding.",
      "oneOf": [
        { "type": "string" },
        { "$ref": "#/$defs/binding" }
      ]
    },
    "value": {
      "$doc": "THE THREE FORMS, and telling them apart is the point of the whole file. A plain string is a STRUCTURAL literal and is emitted verbatim (`inline-block`, `600`, `0.05em`); an object is a TOKEN BINDING; an array is a sequence of both, space-joined. So `[\"1px\", \"solid\", {\"token\": \"content-primary\"}]` records that a border's width and style are structure while its colour is a token — which is a fact no CSS parser can recover, and which is what lets pipeline 2 emit `border border-solid border-content-primary`. A structural literal is NOT an escape hatch for a value that should be a token: the build refuses a colour, a font-size or a non-zero spacing step written as one, because scripts/check-css.mjs reads CSS and would never see it here.",
      "oneOf": [
        { "type": "string" },
        { "$ref": "#/$defs/binding" },
        { "type": "array", "minItems": 2, "items": { "$ref": "#/$defs/term" } }
      ]
    },
    "group": {
      "$doc": "A GROUP of declarations, not one declaration, because the authored CSS groups them and the grouping is a decision — `font-size / font-weight / letter-spacing` on one line is one thought. Pipeline 1 renders one group per line; the key order inside `set` is the emission order.",
      "type": "object",
      "required": ["set"],
      "additionalProperties": false,
      "properties": {
        "note": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "pattern": "^[^\\n]*$" },
          "$doc": "A comment emitted above this group in pipeline 1, one array element per line. It belongs to the declaration it justifies rather than to the component, which is why it is here and not in spec.md."
        },
        "aside": {
          "type": "string",
          "pattern": "^[^\\n]+$",
          "$doc": "A comment emitted at the END of this group's line rather than above it — the form the stylesheet uses for a remark that qualifies one declaration without interrupting the block. Footer's two are the case that added it: `was its own clamp, one 0.5rem cap away from --pad's` sits beside the padding it explains, and hoisting it above would put four lines of preamble in front of a one-line rule. Pipeline 2 drops it: a class list has nowhere to put a comment, and inventing a TSDoc for a remark about a CSS declaration would be a second copy of prose that is already in the definition."
        },
        "set": {
          "type": "object",
          "minProperties": 1,
          "propertyNames": { "pattern": "^-{0,2}[a-z][a-z0-9-]*$" },
          "additionalProperties": { "$ref": "#/$defs/value" }
        }
      }
    },
    "declarations": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/group" } },
    "state": {
      "$doc": "A state hangs off its OWNER, not off the component: `suffix` is appended to whichever selector declares it, so `:hover` on `.btn--solid` is a state of the variant and is emitted directly after it. That is what makes the emission order the cascade.",
      "type": "object",
      "required": ["name", "suffix", "declarations"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "suffix": {
          "$doc": "The pseudo-class or attribute appended to the owner's selector. scripts/emit-tailwind.mjs must have a variant prefix for it or the build fails — a state pipeline 2 cannot name is a state it would silently drop. AN ARRAY IS A SELECTOR LIST: `[\":hover\", \":focus-visible\"]` emits ONE rule under two selectors, which is what the stylesheet already writes wherever a pointer and a keyboard should land on the same appearance. Splitting it into two rules would be the same CSS and a different file; pipeline 2 has no selector list, so it emits each class once per prefix, which is the same cascade.",
          "oneOf": [
            { "type": "string", "pattern": "^(:[a-z][a-z-]*(\\([^)]*\\))?|\\[[a-z-]+\\])$" },
            {
              "type": "array",
              "minItems": 2,
              "uniqueItems": true,
              "items": { "type": "string", "pattern": "^(:[a-z][a-z-]*(\\([^)]*\\))?|\\[[a-z-]+\\])$" }
            }
          ]
        },
        "$doc": { "type": "string" },
        "declarations": { "$ref": "#/$defs/declarations" }
      }
    },
    "rule": {
      "$doc": "The root's own rule: its declarations, and any states of it.",
      "type": "object",
      "required": ["declarations"],
      "additionalProperties": false,
      "properties": {
        "$doc": { "type": "string" },
        "declarations": { "$ref": "#/$defs/declarations" },
        "states": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/state" } }
      }
    },
    "alias": {
      "$doc": "TWO RULES THAT ARE DELIBERATELY IDENTICAL, SAID ONCE. `.btn--solid` and `.btn:hover` carry the same declarations because the outline button's hover IS the solid button at rest — a real relationship that, written twice, reads as duplication somebody should tidy. Declaring the alias makes the relationship the source and the duplication its consequence: both emitters resolve it before rendering, so the bytes are identical either way, and a reader who reaches for the tidy-up finds a statement instead of an accident. It is checked — the referenced state must exist — because an unchecked claim is a comment.",
      "type": "object",
      "required": ["of"],
      "additionalProperties": false,
      "properties": {
        "of": { "const": "base", "$doc": "The only referent the pilot needed. Widen it when a second real case appears, not before." },
        "state": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$", "$doc": "Which state of the referent. Omitted means its resting declarations." },
        "$doc": { "type": "string" }
      }
    },
    "modifier": {
      "$doc": "One variant or one size. It declares EITHER its own declarations OR an alias to another rule's, never both and never neither.",
      "type": "object",
      "required": ["name", "selector"],
      "additionalProperties": false,
      "oneOf": [{ "required": ["declarations"] }, { "required": ["aliases"] }],
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$", "$doc": "The value of the React prop: `variant=\"solid\"`. Renaming it is BREAKING and contract-diff.mjs says so." },
        "selector": { "$ref": "#/$defs/selector" },
        "$doc": { "type": "string" },
        "declarations": { "$ref": "#/$defs/declarations" },
        "aliases": { "$ref": "#/$defs/alias" },
        "states": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/state" } }
      }
    },
    "position": {
      "$doc": "WHERE A RULE SITS AMONG ITS SIBLINGS, from a CLOSED vocabulary. `.case-body h3:first-child` drops the rule above the first heading because a page does not open on a line — a real declaration about position, and the reason it is not a `state` is that a state is something the user does and a position is something the document is. The vocabulary is an enum rather than a selector string precisely so it cannot grow into `:nth-last-child(-n+3):nth-child(3n+1)` by increments; a member is minted by the block that needs it, in the same commit, the way `alias.of` is widened. `first` arrived with case-body. Pipeline 2 renders the whole thing as one arbitrary variant, so the position rides inside `[&…]:` rather than becoming Tailwind's `first:` — same argument as `:hover`, and the same refusal to invent.",
      "type": "object",
      "required": ["name", "at", "declarations"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "at": { "enum": ["first"], "$doc": "`first` → `:first-child`. One member, because one block has asked. Add the next one with the block that needs it and not before — the value of a closed vocabulary is what it refuses." },
        "$doc": { "type": "string" },
        "declarations": { "$ref": "#/$defs/declarations" }
      }
    },
    "part": {
      "$doc": "A companion selector that is not a modifier of the root. R4 MADE A PART A FULL RULE. It used to be declarations and nothing else — no states, one element, no say in the file's shape — and `source` is the block that showed why that was an arbitrary limit rather than a distinction: `.source__link` has a hover-and-focus state, and its canonical HTML puts it on a <button> and an <a>, exactly as Button's does. A part with less power than a root was a fact about the emitter, not about components. What a part still does not have is variants and sizes: a companion selector that needs an axis is a component with its own definition, and that line has held through six blocks. A PART IS ONE OF TWO SHAPES: it has a `selector` of its own, or it is SCOPED — `within` plus `element` — and `link-grid` and `case-body` are the blocks that forced the second. Their children carry no classes at all, and deliberately: case-body's prose is compiled from markdown under content/, so a class per element would put styling inside the content pipeline. The rule that keeps the scoped form from being `.band > .rail--l` in disguise is that BOTH halves are closed — `within` must be the root, and `element` is a list of bare tag names joined by descendant combinators, with no child, sibling or attribute anywhere in it.",
      "type": "object",
      "required": ["name", "declarations"],
      "additionalProperties": false,
      "oneOf": [{ "required": ["selector"] }, { "required": ["within", "element"] }],
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "selector": { "$ref": "#/$defs/selector" },
        "within": {
          "$ref": "#/$defs/selector",
          "$doc": "The ancestor this part is scoped to. STATED here and checked against `root` for the same reason a modifier states its own selector: the CSS stays transcribed rather than assembled, and the two cannot disagree. The root is the only referent — a part scoped to another part would be a two-level descendant chain, which is CSS with more punctuation."
        },
        "element": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9]*$" },
          "$doc": "The element path under `within`, joined by descendant combinators: [\"p\", \"strong\"] is `.case-body p strong`. An ARRAY rather than a string, so the combinator is the emitter's and never the author's — a string would let `p > strong` in through a space."
        },
        "pseudo": {
          "enum": ["::before"],
          "$doc": "A drawn pseudo-element. `::before` is the only one css/components.css uses outside the foundation reset, and it draws the list marker — decoration rather than content, which is exactly why it is here and not in the markup. `::after` is not in the vocabulary because nothing has asked for it."
        },
        "$doc": { "type": "string" },
        "break": {
          "const": true,
          "$doc": "Emit a blank line before this rule. A stylesheet block has paragraphs — `source` separates the list's chrome from the item that repeats inside it — and a paragraph is a statement about which rules belong together. Without this the emitter would have to either lose the break or invent one, and both are worse than recording it."
        },
        "declarations": { "$ref": "#/$defs/declarations" },
        "states": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/state" } },
        "positions": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/position" } }
      }
    },
    "axes": {
      "$doc": "A CLAIM ABOUT THE MATRIX, and the reason it is required rather than optional the moment a component has two axes. `variant` and `size` both apply at once, and in pipeline 2 they land in one class attribute — which has no order. So either every combination is deliberate and the two touch disjoint properties, or somebody has to say which wins, and an emitter must not be the one to decide. Listing them here asserts the first; scripts/emit-react.mjs checks the assertion against its own collision analysis and fails naming both branches and the property if it does not hold. A component with fewer than two axes must omit this: there is no matrix to have an opinion about.",
      "type": "object",
      "required": ["orthogonal"],
      "additionalProperties": false,
      "properties": {
        "$doc": { "type": "string" },
        "orthogonal": {
          "type": "array",
          "minItems": 2,
          "uniqueItems": true,
          "items": { "enum": ["variant", "size"] }
        }
      }
    }
  }
}
